# 2024年6月 C++一级考试真题

## 一、单项选择题（每题2分，共30分）

1. 在C++中,下列不可做变量的是( )。

{{ select(1) }}

- A. five-Star
- B. five_star
- C. fiveStar
- D. _fiveStar

---

2. C++表达式3 - 3 * 3 / 5 的值是( )。

{{ select(2) }}

- A. -1.2
- B. 1
- C. 0
- D. 2

---

3. 在C++中,假设N为正整数,则表达式cout << (N % 3 + N % 7) 可能输出的最大值是( )。

{{ select(3) }}

- A. 6
- B. 8
- C. 9
- D. 10

---

4. C++语句printf("5%%2={%d}\n",5 % 2) 执行后的输出是( )。

{{ select(4) }}

- A. 1={1}
- B. 5%2={5%2}
- C. 5%2={1}
- D. 5 ={1}

---

5. 对整型变量i,执行C++语句cin >> i, cout << i 时如果输入5+2 ,下述说法正确的是( )。

{{ select(5) }}

- A. 将输出整数7
- B. 将输出5
- C. 语句执行将报错,输入表达式不能作为输出的参数
- D. 语句能执行,但输出内容不确定

---

6. 下面C++代码执行后的输出是( )。

```cpp
float a;
a = 101.101;
a = 101;
printf("a+1={%.0f}",a+1);
```

{{ select(6) }}

- A. 102={102}
- B. a+1={a+1}
- C. a+1={102}
- D. a先被赋值为浮点数,后被赋值为整数,执行将报错

---

7. 表达式9/4 - 6 % (6 - 2) * 10 的值是( )。

{{ select(7) }}

- A. -17.75
- B. -18
- C. -14
- D. -12.75

---

8. 下面C++代码执行时输入10后,正确的输出是( )。

```cpp
int N;
cout << "请输入正整数:";
cin >> N;
if (N % 3)
    cin >> N;
printf("第5行代码%2d", N % 3);
else
    printf("第6行代码%2d", N % 3);
```

{{ select(8) }}

- A. 第5行代码1
- B. 第6行代码1
- C. 第5行代码 1
- D. 第6行代码 1

---

9. 下面C++代码执行后输出是( )。

```cpp
int Sum = 0, i = 0;
for ( ; i < 10; )
    cout << i << " " << Sum;
    Sum += i++;
```

{{ select(9) }}

- A. 9 45
- B. 10 55
- C. 10 45
- D. 11 55

---

10. 下面C++代码用于判断N是否为质数(只能被1和它本身整除的正整数)。程序执行后,下面有关描述正确的是( )。

```cpp
int N;
cout << "请输入整数:";
cin >> N;
bool Flag = false;
if (N >= 2){
    Flag = true;
    for (int i=2; i < N; i++)
        if (N % i == 0){
            Flag = false;
            break;
        }
}
if(Flag)
    cout << "是质数";
else
    cout << "不是质数";
```

{{ select(10) }}

- A. 如果输入负整数,可能输出"是质数"
- B. 如果输入2,将输出"不是质数",因为此时循环不起作用
- C. 如果输入2,将输出"是质数",即便此时循环体没有被执行
- D. 如果将if (N >= 2) 改为if (N > 2) 将能正确判断N是否质数

---

11. 下面的C++代码用于求1~N之间所有奇数之和,其中N为正整数,如果N为奇数,则求和时包括N，有关描述错误的是( )。

```cpp
int N;
cout << "请输入正整数:";
cin >> N;
int i = 1, Sum = 0;
while (i <= N){
    if (i % 2 == 1)
        Sum += i;
    i += 1;
}
cout << i << " " << Sum;
```

{{ select(11) }}

- A. 执行代码时如果输入10,则最后一行输出将是11 25
- B. 执行代码时如果输入5,则最后一行输出将是6 9
- C. 将i += 1 移到if (i % 2 == 1) 前一行,同样能实现题目要求
- D. 删除if (i % 2 == 1) ,并将i += 1 改为i += 2 ,同样可以实现题目要求

---

12. 如果一个整数N能够表示为X*X 的形式,那么它就是一个完全平方数,下面C++代码用于完成判断N是否为一个完全平方数,在横线处应填入的代码是( )。

```cpp
int N;
cin >> N;
for(int i = 0; i <= N; i++)
    if(_)
        cout << N << "是一个完全平方数\n";
```

{{ select(12) }}

- A. i == N*N
- B. i*10 == N
- C. i+i == N
- D. i*i == N

---

13. 执行下面C++代码后输出的cnt的值是( )。

```cpp
int cnt=0;
for(int i = 0; i*i < 64; i+=2)
    cnt++;
cout << cnt;
```

{{ select(13) }}

- A. 8
- B. 7
- C. 4
- D. 1

---

14. 小杨父母带他到某培训机构给他报名参加CCF组织的GESP认证考试的第1级,那他可以选择的认证语言有几种?( )

{{ select(14) }}

- A. 1
- B. 2
- C. 3
- D. 4

---

15. ENIAC于1946年投入运行,是世界上第一台真正意义上的计算机,它的主要部件都是( )组成的。

{{ select(15) }}

- A. 感应线圈
- B. 电子管
- C. 晶体管
- D. 集成电路

---

## 二、判断题（每题2分，共20分）

16. 在C++代码中变量X 被赋值为16.44,则cout << X / 10 执行后输出的一定是1 。

{{ select(16) }}

- A. 正确
- B. 错误

---

17. C++的整型变量N 被赋值为10,则语句cout << N / 4 << "->" << N % 4 << "->" << N / 4.0 执行后输出是2->2->2.0 。

{{ select(17) }}

- A. 正确
- B. 错误

---

18. 定义C++的float 型变量N ,则语句cin >> N; cout << int(float(N)) 可以输入正负整数和浮点数,并将其转换为整数后输出。

{{ select(18) }}

- A. 正确
- B. 错误

---

19. C++的整型N 被赋值为5,语句printf("%d*2",N) 执行后将输出10 。

{{ select(19) }}

- A. 正确
- B. 错误

---

20. 在C++中,break 语句用于终止当前层次的循环,循环可以是for 循环,也可以是while 循环。

{{ select(20) }}

- A. 正确
- B. 错误

---

21. 在C++,continue 语句通常与if 语句配合使用。

{{ select(21) }}

- A. 正确
- B. 错误

---

22. 在C++代码中,不可以将变量命名为printf ,因为printf 是C++语言的关键字。

{{ select(22) }}

- A. 正确
- B. 错误

---

23. 在C++中有整型变量N,则表达式 (N+=8 / 4 / 2) 相当于 (N+=8 /(4 / 2)) 。

{{ select(23) }}

- A. 正确
- B. 错误

---

24. C++中定义变量int N ,则表达式(!!N) 的值也是N 的值。

{{ select(24) }}

- A. 正确
- B. 错误

---

25. GESP测试是对认证者的编程能力进行等级认证,同一级别的能力基本上与编程语言无关。

{{ select(25) }}

- A. 正确
- B. 错误

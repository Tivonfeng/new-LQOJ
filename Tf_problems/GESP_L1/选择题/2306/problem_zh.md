# C++基础知识综合测试

## 一、单选题（每题2分，共30分）

1. 以下不属于计算机输出设备的有( )。

{{ select(1) }}

- A. 麦克风
- B. 音箱
- C. 打印机
- D. 显示器

---

2. ChatGPT 是OpenAI 研发的聊天机器人程序，它能通过理解和学习人类的语言来进行对话，还能根据聊天的上下文进行互动，完成很多工作｡请你猜猜看，下面任务中，ChatGPT 不能完成的是( )。

{{ select(2) }}

- A. 改邮件
- B. 编剧本
- C. 擦地板
- D. 写代码

---

3. 常量'3'的数据类型是( )。

{{ select(3) }}

- A. double
- B. float
- C. char
- D. int

---

4. 下列关于C++语言变量的叙述，正确的是( )。

{{ select(4) }}

- A. 变量可以没有定义
- B. 对一个没有定义的变量赋值，相当于定义了一个新变量
- C. 执行赋值语句后，变量的类型可能会变化
- D. 执行赋值语句后，变量的值可能不会变化

---

5. 以下可以作为C++标识符的是( )。

{{ select(5) }}

- A. number_of_Chinese_people_in_millions
- B. 360AntiVirus
- C. Man&Woman
- D. break

---

6. 以下哪个不是C++语言的关键字?( )

{{ select(6) }}

- A. double
- B. else
- C. while
- D. endl

---

7. 如果a、b 和 c 都是int 类型的变量，下列哪个语句不符合C++语法?( )

{{ select(7) }}

- A. a=(b==c);
- B. b=5.5;
- C. c=a+b+c;
- D. a+c=b+c;

---

8. 如果用一个int 类型的变量a 表达正方形的边长，则下列哪个表达式不能用来计算正方形的面积?( )

{{ select(8) }}

- A. a * a
- B. 1 * a * a
- C. a ∧ 2
- D. a * 2 * a / 2

---

9. 表达式 (4 *(11+12) / 4) 的计算结果为()。

{{ select(9) }}

- A. 47
- B. 20
- C. 23
- D. 56

---

10. 如果a 为int 类型的变量，且a 的值为6，则执行 a %= 4 之后，a 的值会是( )。

{{ select(10) }}

- A. 1
- B. 2
- C. 3
- D. 4

---

11. 如果a 和b 均为int 类型的变量，下列表达式能正确判断"a 等于0 且b 等于0"的是( )。

{{ select(11) }}

- A. (a==b==0)
- B. !(a || b)
- C. (a+b==0)
- D. (a==0)+(b==0)

---

12. 如果a 和b 为int 类型的变量，且值分别为7 和2，则下列哪个表达式的计算结果不是3.5?( )

{{ select(12) }}

- A. 0.0+a / b
- B. (a+0.0) / b
- C. (0.0+a) / b
- D. a / (0.0 + b)

---

13. 在下列代码的横线处填写( )，使得输出是"20 10"。

```cpp
#include <iostream>
using namespace std;
int main(){
int a = 10,b= 20;
a= b=a+b; _;//在此处填入代码
a =b-a;
cout <<a<<" "<<b<< endl;
return 0;
}
```

{{ select(13) }}

- A. a + b
- B. b
- C. a - b
- D. b - a

---

14. 在下列代码的横线处填写( )，可以使得输出是"147"。

```cpp
#include<iostream>
using namespace std;
int main(){
for (int i=1;i <= 8; i++)
if (_)//在此处填入代码
cout<<<i;
return 0;
}
```

{{ select(14) }}

- A. i % 2 == 1
- B. i % 3 == 1
- C. i = i + 3
- D. i + 3

---

15. 执行以下C++语言程序后，输出结果是( )。

```cpp
#include <iostream>
using namespace std;
int main(){
int sum;
for (int i=1;i<=20;i++)
if (i%3==0||i%5== 0)
sum+=i;
cout << sum <<endl;
return 0;
}
```

{{ select(15) }}

- A. 63
- B. 98
- C. 113
- D. 无法确定

---

## 二、判断题（每题2分，共20分）

16. 计算机硬件主要包括运算器、控制器、存储器、输入设备和输出设备。

{{ select(16) }}

- A. 正确
- B. 错误

---

17. 诞生于1958 年的103 机是中国第一台通用数字电子计算机，比1946 年在美国诞生的第一台通用电子计算机ENIAC 晚了十多年。

{{ select(17) }}

- A. 正确
- B. 错误

---

18. 在C++语言中，计算结果必须存储在变量中才能输出。

{{ select(18) }}

- A. 正确
- B. 错误

---

19. 在C++语言中，标识符的命名不能完全由数字组成，至少有一个字母就可以。

{{ select(19) }}

- A. 正确
- B. 错误

---

20. 10 是一个int 类型常量。

{{ select(20) }}

- A. 正确
- B. 错误

---

21. if 语句可以没有else 子句。

{{ select(21) }}

- A. 正确
- B. 错误

---

22. do ... while 语句的循环体至少会执行一次。

{{ select(22) }}

- A. 正确
- B. 错误

---

23. 如果a 和b 为int 类型的变量，则表达式 a=b 可以判断a 和b 是否相等。

{{ select(23) }}

- A. 正确
- B. 错误

---

24. 如果a 为int 类型的变量，则表达式 (a % 4==2 ) 可以判断a 的值是否为偶数。

{{ select(24) }}

- A. 正确
- B. 错误

---

25. 表达式(37 / 4)的计算结果为9，且结果类型为int。

{{ select(25) }}

- A. 正确
- B. 错误

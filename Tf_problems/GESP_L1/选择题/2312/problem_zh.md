# 2023年12月 C++一级考试真题

## 一、单项选择题（每题2分，共30分）

1. 以下C++不可以作为变量的名称的是( )。

{{ select(1) }}

- A. CCF GESP
- B. ccfGESP
- C. CCFgesp
- D. CCF_GESP

---

2. C++表达式10 - 3 * (2 + 1) % 10 的值是( )。

{{ select(2) }}

- A. 0
- B. 1
- C. 2
- D. 3

---

3. 假设现在是上午十点,求出N小时(正整数)后是第几天几时,如输入20小时则为第2天6点,如N输入4则为今天14点。为实现相应功能,应在横线处填写代码是( )。

```cpp
int N,dayX,hourX;
cin >>N;
dayX= _, hourX;
if (dayX== 0)
    cout<<"今天"<<hourX<<"点";
else
    cout<<"第"<<(dayX+1)<<"天"<<hourX<<"点";
```

{{ select(3) }}

- A. (10 + N) % 24 , (10 + N) / 24
- B. (10 + N) / 24 , (10 + N) % 24
- C. N % 24 , N / 24
- D. 10 / 24 , 10 % 24

---

4. 下面的程序用于判断N是否为偶数,横线处应填写代码是( )

```cpp
cin >> N;
if ( _ )
    cout<<"偶数";
else
    cout<<"奇数";
```

{{ select(4) }}

- A. N % 2 == 0
- B. N % 2 = 0
- C. N % 2
- D. N % 2 != 0

---

5. 下面对C++代码执行后输出的描述,正确的是( )。

```cpp
cin >> N;
cnt=0;
for(int i=1;i<N;i++)
    cout << cnt;
    cnt +=1;
```

{{ select(5) }}

- A. 如果输入的N是小于等于2整数,第5行将输出0。
- B. 如果输入的N是大于等于2整数,第5行将输出N-1。
- C. 如果输入的N是大于等于2整数,第5行将输出N。
- D. 以上说法均不正确。

---

6. 下面C++代码执行后的输出是( )。

```cpp
cnt=0;
for(int i=1;i<10;i++){
    cnt += 1;
    i+=2;
}
cout << cnt;
```

{{ select(6) }}

- A. 10
- B. 9
- C. 3
- D. 1

---

7. 下面C++代码执行后的输出是( )。

```cpp
cnt=0;
for(int i=1;i<20;i++){
    if(i%2) continue;
    else if(i%3==0 && i%5==0) {
        cnt += i;
        break;
    }
}
cout << cnt;
```

{{ select(7) }}

- A. 90
- B. 44
- C. 20
- D. 10

---

8. 下面C++代码执行后的输出是( )。

```cpp
N=10;
cnt=0;
while(1){
    if(N == 0) break;
    cnt += 1;
    N-=2;
}
cout << cnt;
```

{{ select(8) }}

- A. 11
- B. 10
- C. 5
- D. 4

---

9. 下面C++代码用于判断一个数是否为质数(素数),在横线处应填入代码是( )。

```cpp
cin>>N;
cnt=0;
for (int i=1;i<N+1;i++)
    if(N%i==0)
        _;
if(cnt==2)
    cout<<N<<"是质数。";
else
    cout<<N<<"不是质数。";
```

{{ select(9) }}

- A. cnt = 1
- B. cnt = 2
- C. cnt =+ 1
- D. cnt += 1

---

10. 对下面的代码,描述正确的是( )。

```cpp
#include <iostream>
using namespace std;
int main(){
    int arr[]={2,6,3,5,4,8,1,0,9,10};
    for(int i=0;i<10;i++)
        cout << arr[i]<<" ";
    cout << i << endl;
    return 0;
}
```

{{ select(10) }}

- A. 输出2 6 3 5 4 8 1 0 9 10 10
- B. 输出2 6 3 5 4 8 1 0 9 9
- C. 输出2 6 3 5 4 8 1 0 9 10
- D. 提示有编译错误

---

11. 执行下面C++代码后输出是( )。

```cpp
cnt=0;
for(i=10;i>3;i-=3)
    cout << cnt;
    cnt= cnt +i;
```

{{ select(11) }}

- A. 3
- B. 21
- C. 27
- D. 49

---

12. 定义变量char c,下面对c 赋值的语句,不符合语法的是( )。

{{ select(12) }}

- A. c = (char)66;
- B. c = (char)(66);
- C. c = char(66);
- D. c = char 66;

---

13. 下图所示C++代码对大写字母'A' 到'Z' 分组,对每个字母输出所属的组号,那么输入'C' 时将输出的组号是?( )

```cpp
char c;
while (1){
    cin >> c;
    if(c=='q') break;
    switch(c){
        case 'A': cout <<"1 ";break;
        case 'B':cout <<"3";
        case 'C': cout <<"3";
        case 'D': cout <<"5";break;
        case 'E':cout <<"5";break;
        default: cout <<"9";
    }
}
cout << endl;
```

{{ select(13) }}

- A. 3
- B. 3 5
- C. 3 5 9
- D. 以上都不对

---

14. 某公司新出了一款无人驾驶的小汽车,通过声控智能驾驶系统,乘客只要告诉汽车目的地,车子就能自动选择一条优化路线,告诉乘客后驶达那里。请问下面哪项不是驾驶系统完成选路所必须的( )。

{{ select(14) }}

- A. 麦克风
- B. 扬声器
- C. 油量表
- D. 传感器

---

15. 现代计算机是指电子计算机,它所基于的是( )体系结构。

{{ select(15) }}

- A. 艾伦·图灵
- B. 冯·诺依曼
- C. 阿塔纳索夫
- D. 埃克特-莫克利

---

## 二、判断题（每题2分，共20分）

16. C++表达式int(3.14) 的值为3。

{{ select(16) }}

- A. 正确
- B. 错误

---

17. C++语句cout << (2 * 3, 3 % 10, 2+3) 的输出为6,3,5 。

{{ select(17) }}

- A. 正确
- B. 错误

---

18. for(int i = 1; i < 10; i += 3); 表示i从1开始到10结束间隔为3,相当于1、4、7、10。

{{ select(18) }}

- A. 正确
- B. 错误

---

19. C++表达式010+100+001的值为111。

{{ select(19) }}

- A. 正确
- B. 错误

---

20. if 语句中的条件表达式的结果可以为int 类型。

{{ select(20) }}

- A. 正确
- B. 错误

---

21. 在下面的C++代码while(1) continue; 中,由于循环中的continue 是无条件被执行,因此将导致死循环。

{{ select(21) }}

- A. 正确
- B. 错误

---

22. C++表达式 ('1'+'1'=='2'? flag=1:flag=2) 的结果值和表达式 (flag==2) 的相同。

{{ select(22) }}

- A. 正确
- B. 错误

---

23. 在C++的程序中,不能用scanf 作为变量名。

{{ select(23) }}

- A. 正确
- B. 错误

---

24. 小杨最近在准备考GESP,他用的Dev C++来练习和运行程序,所以Dev C++也是一个小型操作系统。

{{ select(24) }}

- A. 正确
- B. 错误

---

25. 任何一个while 循环都可以转化为等价的for 循环。

{{ select(25) }}

- A. 正确
- B. 错误

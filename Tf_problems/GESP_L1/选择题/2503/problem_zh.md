# 2025年3月 C++一级考试真题

## 一、单项选择题（每题2分，共30分）

1. 2025年春节，DeepSeek横空出世，《哪吒2》票房惊人。下面描述成立的是( )。

{{ select(1) }}

- A. 《哪吒2》是一款新型操作系统
- B. DeepSeek是深海钻探软件
- C. 《哪吒2》可以生成新的软件
- D. DeepSeek可以根据《哪吒2》的场景生成剧情脚本

---

2. 在某集成开发环境中编辑源代码文件时不可以执行的操作是( )。

{{ select(2) }}

- A. 修改变量定义
- B. 保存代码修改
- C. 撤销代码修改
- D. 插入执行截图

---

3. 在C++中，下列可以做变量的是( )。

{{ select(3) }}

- A. Var-1
- B. $1
- C. %%1
- D. _Var_1

---

4. 以下哪个是C++语言的关键字?( )

{{ select(4) }}

- A. abs
- B. cin
- C. do
- D. endl

---

5. 下面框架在L1和L2处填写代码，哪组不能通过编译( )。

```cpp
int main() { int i = 0;
    // L1
    break;
    // L2
    return 0;
}
```

{{ select(5) }}

- A. do { } while (i != 0);
- B. for (; i < 10; i++) { }
- C. if (i == 0) { }
- D. switch (i) { }

---

6. 下面C++语句执行后的输出是__0322$$的是( )。

{{ select(6) }}

- A. printf("__ %2d%02d$$", 3, 22)
- B. printf("__ %02d%2d$$", 3, 22)
- C. printf("__ %02d%02d$$$$", 3, 22)
- D. printf("____ %02d%02d$$$$", 3, 22)

---

7. 有关C++代码`printf("我爱码代码!");`的说法，错误的是( )。

{{ select(7) }}

- A. 汉字改为英文Hello，代码能正确执行
- B. 汉字改为Hello代码!，代码能正确执行
- C. 每个双引号改为两个单引号
- D. 每个双引号改为三个双引号

---

8. C++表达式16 / 4 % 2 的值是( )。

{{ select(8) }}

- A. 8
- B. 4
- C. 2
- D. 0

---

9. N是正整数（值为12），则cout << (N % 3 + N / 5) 的输出是( )。

{{ select(9) }}

- A. 6.4
- B. 2.4
- C. 6
- D. 2

---

10. 下面C++代码执行后的输出是( )。

```cpp
int N = 10;
printf("{N}*{N}={%d*%d}", N, N, N * N);
```

{{ select(10) }}

- A. 10*10={10*10}
- B. 100=10
- C. N*N=100
- D. {N}*{N}={10*10}

---

11. 执行下面C++代码，输入100和200，输出是( )。

```cpp
cout << "请输入第1个正整数:"; int first,second;
cin >> first;
cout << "请输入第2个正整数:";
cin >> second;
cout << (first / second * second) << endl;
```

{{ select(11) }}

- A. 200
- B. 100
- C. 1
- D. 0

---

12. 下面C++代码用于输出能被2整除且除以7余数为2的数，不能实现的是( )。

```cpp
for (int i = 0; i < 100; i++)
    if(_______________________) cout << i << " ";
```

{{ select(12) }}

- A. ((i % 2 == 0) && (i % 7 == 2))
- B. ((!(i % 2)) && (i % 7 == 2))
- C. ((!(i % 2)) && (!(i % 7)))
- D. ((i % 2 != 1) && (i % 7 == 2))

---

13. 下面C++代码执行后输出是( )。

```cpp
int tnt = 0;
for (int i = -1000; i < 1000; i++)
    tnt += i;
cout << tnt << endl;
```

{{ select(13) }}

- A. -1000
- B. 0
- C. 999
- D. 1000

---

14. 下面C++代码执行后输出的是( )。

```cpp
int i;
for (i = 1; i < 100; i += 5)
    continue;
cout << i << endl;
```

{{ select(14) }}

- A. 101
- B. 100
- C. 99
- D. 96

---

15. 下面C++代码执行后输出的是( )。

```cpp
int tnt = 0;
for (int i = 5; i < 100; i += 5){
    if (i % 2 == 0)
        continue;
    tnt += 1;
    if (i % 3 == 0 && i % 7 == 0)
        break;
}
cout << tnt << endl;
```

{{ select(15) }}

- A. 500
- B. 450
- C. 10
- D. 1

---

## 二、判断题（每题2分，共20分）

16. C++、Python都是高级编程语言，它们的每条语句最终都要通过机器指令来完成。

{{ select(16) }}

- A. 正确
- B. 错误

---

17. 在C++中表达式N * 2 % N（N为正整数）的值为2。

{{ select(17) }}

- A. 正确
- B. 错误

---

18. 执行代码：char N; printf("请输入正整数:"); cin >> N; printf("%d\n", N * 2); 输入10后输出20。

{{ select(18) }}

- A. 正确
- B. 错误

---

19. 删除代码中的continue不影响执行效果：

```cpp
for (int i = 0; i < 100; i++){
    if (i % 2 == 0){
        printf("偶数");
        continue;
    }
    else printf("奇数");
}
```

{{ select(19) }}

- A. 正确
- B. 错误

---

20. 代码for (int _ = 0; _ < 100; _++) cout << "*" << endl; 执行报错，因_不能做变量名。

{{ select(20) }}

- A. 正确
- B. 错误

---

21. 代码for (int i = 3; i < 5; i += 2) printf("%d ", i); 执行后先后输出3和5。

{{ select(21) }}

- A. 正确
- B. 错误

---

22. 代码for (int i = 0; i < 10; i++) cout << (i * 2 < i * i) << " "; 执行后输出7个true。

{{ select(22) }}

- A. 正确
- B. 错误

---

23. 在C++代码中，user_Name、_userName、user-Name、userName_都是合法的变量名。

{{ select(23) }}

- A. 正确
- B. 错误

---

24. C++语言中continue语句可以提前结束循环。

{{ select(24) }}

- A. 正确
- B. 错误

---

25. C++中定义整型变量N，执行scanf("%d", &N); cout << N / 3 * 5; 输入3.6则输出6。

{{ select(25) }}

- A. 正确
- B. 错误

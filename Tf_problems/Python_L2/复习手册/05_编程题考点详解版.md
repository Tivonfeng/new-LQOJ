# Python 二级编程题核心考点分析与集训计划

> 基于 `Tf_problems/Python_L2/编程题/1.md` 共 15 道编程题深度分析
>
> **适用对象**：Python 二级考生
> **目标**：掌握所有编程题题型，突破实操关

---

## 📊 考点分布统计

| 考点类别 | 题目数量 | 占比 | 代表题目 |
|---------|---------|------|---------|
| 条件判断 | 5 | 33% | 成绩分级、小学生体能测试 |
| 循环结构 | 5 | 33% | 空调开关、高温预警 |
| 列表操作 | 3 | 20% | 成绩查询、双一流学员 |
| 字符串处理 | 3 | 20% | 检票程序、单词检测 |
| 数学计算 | 1 | 7% | 长方形计算器 |

---

## 🔥 核心考点详解

### 1️⃣ 条件判断（**最高频考点**）

**题型特点**：根据不同条件执行不同逻辑（不需要循环）

| 题目 | 条件类型 | 判断要点 |
|-----|---------|---------|
| 成绩分级 | 多分支区间 | 90/80/60 分界点 |
| 小学生体能测试 | 多分支组合 | 年级(1-3/4-6) × 性别(男/女) |
| AI智能聊天机器人 | 多条件优先级 | "你好" > "再见" > "谢谢" |

**模板代码**：

```python
# 多分支判断（if-elif链）
score = int(input())
if score >= 90:
    print("A")
elif score >= 80:
    print("B")
elif score >= 60:
    print("C")
else:
    print("D")

# 组合条件判断（年级×性别）
grade = int(input())
gender = input()
if 1 <= grade <= 3:
    if gender == "男":
        print("引体向上")
    else:
        print("仰卧起坐")
elif 4 <= grade <= 6:
    if gender == "男":
        print("800米跑步")
    else:
        print("500米跑步")

# 优先级判断（关键词匹配）
text = input()
if "你好" in text:
    print("你好呀！有什么我可以帮忙的？")
elif "再见" in text:
    print("再见啦！祝你有个好心情！")
elif "谢谢" in text:
    print("不客气呀！能帮到你我很开心。")
else:
    print("不太明白你的意思呢")
```

**易错点**：
- 区间边界：`>=` 和 `<=` 要区分清楚
- 条件优先级：`elif` 从上到下依次判断
- Python 特有的连续比较：`90 <= score <= 100`

---

### 2️⃣ 循环结构（**必考题型**）

**题型特点**：重复执行直到满足退出条件

| 题目 | 循环类型 | 退出机制 | 涉及知识 |
|-----|---------|---------|---------|
| 空调智能开关 | while True | `break` + 温度条件 | 条件判断 |
| 高温预警机制 | for 遍历 | 自然结束 | 计数统计 |
| 输出截止字符 | for 遍历 | `break` + 字符匹配 | 字符串遍历 |
| 自动分拣土豆机 | while True | 输入为0时结束 | 条件判断、数学计算 |
| 智能抽奖机 | for + break | 次数限制或猜中 | 条件判断 |

**模板代码**：

```python
# 无限循环 + break 退出（空调开关）
while True:
    temp = float(input("请输入当前的温度"))
    if temp > 35:
        print("空调已打开")
        break

# for 遍历列表 + 计数（高温预警）
data = input("请输入一周的温度：")
temps = data.split(",")
count = 0
for t in temps:
    t_int = int(t.strip())
    if t_int >= 35:
        count += 1
if count >= 3:
    print(f"高温红色警报！{count}天超35℃")
else:
    print("正常")

# 遍历字符串 + break（截止字符）
text = "Reading a good book, like and many noble people talk"
stop = input("请输入截止字符：")
for char in text:
    if char == stop:
        break
    print(char, end="")

# while True + break + 数学计算（土豆机）
prices = []
while True:
    weight = int(input())
    if weight == 0:
        break
    # 根据重量判断类别并计算价格
    if weight >= 50:
        price = weight * 1.5 / 500
    elif weight >= 30:
        price = weight * 1.2 / 500
    elif weight >= 20:
        price = weight * 1.0 / 500
    else:
        price = weight * 0.7 / 500
    prices.append(price)

# for 次数限制（抽奖机）
import random
secret = random.randint(1, 100)
for i in range(5):
    guess = int(input("请输入数字"))
    if guess == secret:
        print("恭喜你获得大奖！")
        break
    else:
        print("很遗憾，再来一次吧")
```

**易错点**：
- `while True` 必须配合 `break` 使用，否则死循环
- `range(n)` 生成 0 到 n-1
- 循环内变量要及时更新

---

### 3️⃣ 列表操作（**高频考点**）

**题型特点**：存储有序数据，通过索引访问或排序

| 题目 | 操作类型 | 核心方法 |
|-----|---------|---------|
| 成绩查询程序 | 单索引访问 | `scores[id-1]` |
| 双一流学员 | 多列表遍历 | 索引遍历 |
| 运动统计 | 遍历求和 + 排序 | `sorted()` |

**模板代码**：

```python
# 列表索引访问（成绩查询）
scores = [91, 89, 92, 87, 94, 97, 100, 88, 92, 94,
          91, 90, 93, 96, 98, 93, 89, 95, 94, 99]
id = int(input("请输入学号："))
if 1 <= id <= 20:
    print(f"该学生的成绩为{scores[id-1]}。")
else:
    print("该学号不存在！")

# 索引遍历多列表（双一流学员）
Name = ['豆豆', '小明', '呆宝', '小玉', '小五', '刀仔']
Math = [96, 77, 93, 99, 84, 81]
Chinese = [86, 87, 95, 90, 92, 94]
top_students = []
for i in range(len(Name)):
    if Math[i] >= 90 and Chinese[i] >= 90:
        top_students.append(Name[i])
print(top_students)

# 列表遍历 + 求和 + 排序（运动统计）
monday = [4125, 3947, 15395, 238, 6844, 8341, 3419, 2943]
tuesday = [3975, 3584, 9754, 4584, 3435, 9890, 2344, 9890]
total = []
for i in range(len(monday)):
    total.append(monday[i] + tuesday[i])
total_sorted = sorted(total)
print(total_sorted)
```

**易错点**：
- 索引从 0 开始，输入学号要减 1
- `sorted()` 返回新列表，`sort()` 原地修改
- 多列表遍历时注意各列表长度一致

---

### 4️⃣ 字符串处理（**高频考点**）

**题型特点**：解析和操作文本数据

| 题目 | 操作类型 | 核心方法 |
|-----|---------|---------|
| 检票程序 | 字符串索引 | `ticket[0]`, `ticket[1]` |
| 飞花令 | 字符串分割 | `split()` |

**模板代码**：

```python
# 字符串索引解析（检票程序）
while True:
    ticket = input("请输入您的球票编码：")
    if ticket == "0":
        break
    if len(ticket) == 5 and ticket[0] in ['A', 'B', 'C'] and ticket[1] in ['1', '2', '3']:
        area = ticket[0]
        floor = ticket[1]
        seat = ticket[2:]
        print(f"您的观看区域是：{area}区{floor}楼{seat}号座位")
    else:
        print("您的票号输入有误，请重新输入！")

# 字符串分割 + 格式化输出（飞花令）
keyword = input("请输入本局的雅令：")
poems = input("输入五言或七言诗句，不同诗句用'、'隔开：")
poem_list = poems.split("、")
score = 0
for poem in poem_list:
    count = 0
    for char in poem:
        if char == keyword:
            count += 1
    score += count
print(f"你得了{score}分！")
```

**易错点**：
- 字符串索引从 0 开始
- `split()` 默认按空格分割，指定分隔符要加参数
- 字符串不可修改，只能创建新字符串

---

### 5️⃣ 数学计算（**中等频次**）

**题型特点**：应用数学公式计算

| 题目 | 计算类型 | 公式 |
|-----|---------|-----|
| 长方形计算器 | 几何计算 | `周长=2×(长+宽)`, `面积=长×宽` |

**模板代码**：

```python
# 浮点数输入 + 格式化输出
length = float(input("请输入长："))
width = float(input("请输入宽："))
if length <= 0 or width <= 0:
    print("输入有误，请重新输入！")
else:
    perimeter = 2 * (length + width)
    area = length * width
    print(f"周长是：{perimeter:.2f}")
    print(f"面积是：{area:.2f}")
```

---

### 6️⃣ 字符串判断（**基础考点**）

**题型特点**：遍历字符串，判断是否有重复字符

| 题目 | 操作 | 原理 |
|-----|------|-----|
| 单词重复字母检测 | 双重循环 | 逐个字符比较是否重复 |

**模板代码**：

```python
# 双重循环判断重复字符
word = input()
has_repeat = False
for i in range(len(word)):
    for j in range(i + 1, len(word)):
        if word[i] == word[j]:
            has_repeat = True
            break
    if has_repeat:
        break

if has_repeat:
    print("no")    # 有重复字母
else:
    print("yes")   # 无重复字母
```

---

## 📈 难度分布

| 难度 | 题目特征 | 占比 | 代表题目 |
|-----|---------|------|---------|
| ⭐ 基础 | 单条件 + 简单输出 | 33% | 成绩分级、单词检测 |
| ⭐⭐ 中等 | 循环 + 条件 | 40% | 空调开关、检票程序 |
| ⭐⭐⭐ 较难 | 复合逻辑 + 多知识点 | 27% | 飞花令、高温预警 |

---

## 🎯 备考建议

### 必须掌握的核心技能

1. **条件判断**（必须精通）
   - 单分支 `if`
   - 双分支 `if-else`
   - 多分支 `if-elif-else`
   - 组合条件判断

2. **循环结构**（必须精通）
   - `for` 遍历列表
   - `while True + break` 退出模式
   - `range()` 函数

3. **列表操作**（必须掌握）
   - 索引访问
   - 多列表遍历（用索引）
   - `sorted()` 排序

4. **字符串处理**（必须掌握）
   - 索引和切片
   - `split()` 分割
   - `in` 子串检查

### 易错点提醒

| 易错点 | 示例 | 正确做法 |
|-------|-----|---------|
| input 返回字符串 | `age = input()` → `"18"` | `age = int(input())` |
| 索引从0开始 | `list[1]` 实际第2个 | 第一个元素用 `list[0]` |
| 浮点数比较 | `0.1 + 0.2 == 0.3` → False | 避免直接比较浮点数 |
| 字符串 vs 数字 | `"10" + 1` → 报错 | 统一类型后运算 |
| 循环忘记 break | while True 无退出 | 明确退出条件 |

---

## 📋 6 小时集训计划

### ⏰ 第一时段：条件判断（0:00-1:00）

| 题目 | 训练目标 | 建议时间 |
|-----|---------|---------|
| 成绩分级 | 多分支判断 | 15分钟 |
| 小学生体能测试 | 组合条件 | 15分钟 |
| AI智能聊天机器人 | 优先级逻辑 | 20分钟 |

---

### ⏰ 第二时段：循环结构（1:00-2:00）

| 题目 | 训练目标 | 建议时间 |
|-----|---------|---------|
| 空调智能开关 | while+break | 15分钟 |
| 高温预警机制 | for遍历+计数 | 20分钟 |
| 自动分拣土豆机 | while+break+计算 | 25分钟 |

---

### ⏰ 第三时段：列表与字符串（2:00-3:00）

| 题目 | 训练目标 | 建议时间 |
|-----|---------|---------|
| 成绩查询程序 | 列表索引 | 15分钟 |
| 双一流学员 | 多列表遍历 | 20分钟 |
| 检票程序 | 字符串索引 | 20分钟 |
| 运动统计 | 列表运算+排序 | 15分钟 |

---

### ⏰ 第四时段：综合应用（3:00-4:00）

| 题目 | 训练目标 | 建议时间 |
|-----|---------|---------|
| 飞花令 | 字符串+列表综合 | 25分钟 |
| 智能抽奖机 | for循环+条件 | 20分钟 |
| 输出截止字符 | 字符串遍历+break | 15分钟 |

---

### ⏰ 第五时段：数学与字符串判断（4:00-5:00）

| 题目 | 训练目标 | 建议时间 |
|-----|---------|---------|
| 长方形计算器 | 浮点计算+格式化 | 20分钟 |
| 单词重复字母检测 | 循环判断 | 15分钟 |
| 查漏补缺 | 薄弱环节 | 25分钟 |

---

### ⏰ 第六时段：全真模拟（5:00-6:00）

**限时训练**：从 15 道题中随机抽取 5 道，限时完成

| 题目难度 | 建议时间 | 策略 |
|---------|---------|------|
| 基础题 | 5-8分钟/道 | 确保正确 |
| 中等题 | 10-12分钟/道 | 思考后编码 |
| 较难题 | 15分钟/道 | 先跳过 |

---

## 📝 配套练习题清单

### 必练基础题（⭐）

| 序号 | 题目 | 考点 | 建议时间 |
|-----|------|-----|---------|
| 1 | 成绩分级 | 条件判断-多分支 | 15分钟 |
| 2 | 成绩查询程序 | 列表操作-索引 | 10分钟 |
| 3 | 单词重复字母检测 | 循环判断 | 10分钟 |
| 4 | 长方形计算器 | 数学计算 | 15分钟 |

### 必练中等题（⭐⭐）

| 序号 | 题目 | 考点 | 建议时间 |
|-----|------|-----|---------|
| 5 | 小学生体能测试 | 条件判断-组合 | 15分钟 |
| 6 | 智能抽奖机 | 循环结构-for | 15分钟 |
| 7 | 空调智能开关 | 循环结构-while | 15分钟 |
| 8 | 检票程序 | 字符串处理 | 20分钟 |
| 9 | 双一流学员 | 列表操作-多列表遍历 | 20分钟 |

### 进阶挑战题（⭐⭐⭐）

| 序号 | 题目 | 考点 | 建议时间 |
|-----|------|-----|---------|
| 10 | 高温预警机制 | 循环结构-for+计数 | 25分钟 |
| 11 | 自动分拣土豆机 | 循环+条件+计算 | 25分钟 |
| 12 | 飞花令 | 字符串+列表 | 25分钟 |
| 13 | AI智能聊天机器人 | 条件判断-优先级 | 20分钟 |
| 14 | 输出截止字符 | 循环结构-遍历break | 15分钟 |

---

## 🎯 培训效果评估

### 达标标准

| 考点 | 正确率要求 |
|-----|-----------|
| 条件判断 | ≥ 90% |
| 循环结构 | ≥ 85% |
| 列表操作 | ≥ 85% |
| 字符串处理 | ≥ 80% |
| 综合模拟 | ≥ 75% |

---

## 💡 考试技巧

### 时间分配策略

| 题目难度 | 建议时间 | 策略 |
|---------|---------|------|
| 基础题 | 5-8分钟 | 确保正确，仔细检查 |
| 中等题 | 10-15分钟 | 思考清楚再编码 |
| 较难题 | 15分钟 | 先跳过，完成后再回来 |

### 调试技巧

```python
# 1. 输出中间结果
print(f"i={i}, value={value}")

# 2. 小数据测试
# 先用 n=2 或 n=3 验证逻辑

# 3. 边界测试
# 输入：最小值、0、空输入
```

### 得分策略

```
✓ 暴力解法也能过（不必追求最优）
✓ 注意输出格式：空格、换行
✓ 保留小数位数要精确
```

---

> **注**：以上编程题均可在 `./Tf_problems/Python_L2/编程题/1.md` 文件中找到详细题目描述和参考代码。

# 2024年12月 Python二级考试真题

## 一、单选题（每题2分，共50分）

1. 已知字典如下：

```python
dic1={
'name':'Ming',
'age':20,
'grade':'A',
'Tel':'6666666'
}
```

以下哪个代码运行结果为20？

{{ select(1) }}

- A. dic1('age')
- B. dic1[1]
- C. dic1(20)
- D. dic1['age']

---

2. 若`ls=["cat","dog","tiger","fox"]`，则运行代码`ls[2]`的结果是？

{{ select(2) }}

- A. ["cat"]
- B. ["dog"]
- C. "tiger"
- D. ["fox"]

---

3. 有代码`city = ["Qingdao","Beijing","Shanghai","Wuhu","Hefei"]`，则正确获取`city`元素个数的命令是？

{{ select(3) }}

- A. city.clear()
- B. city.copy()
- C. city.reverse()
- D. len(city)

---

4. 已知`d={"东岳":"泰山","西岳":"华山","南岳":"衡山","北岳":"恒山"}`，则向`d`中添加元素"中岳-嵩山"的选项是？

{{ select(4) }}

- A. d.pop("中岳")
- B. d.get("中岳")
- C. d["中岳"]="嵩山"
- D. del d["中岳"]

---

5. 已知列表`ver=["香菱",1,"钟离",28,"芭芭拉","神里华"]`，下列哪个命令能够获得列表中的"芭芭拉"？

{{ select(5) }}

- A. ver[5]
- B. ver[3]
- C. ver[-2]
- D. ver[-1]

---

6. 小宸想统计某个人名在年段学生姓名列表中的出现次数，可使用以下哪个函数？

{{ select(6) }}

- A. append()
- B. insert()
- C. count()
- D. index()

---

7. 运行以下代码的结果是？

```python
s1="Let life be beautiful like summer flowers and death like autumn leaves."
s2=s1.split()
print(type(s2))
```

{{ select(7) }}

- A. <class 'set'>
- B. <class 'str'>
- C. <class 'list'>
- D. <class 'dict'>

---

8. 已知：

```python
a=[70,71,72,73,74,75]
b=('2021年','2022年','2023年','2024年')
c=["新中国","旧中国"]
```

以下哪个选项可输出"2024年是新中国成立75周年｡"？

{{ select(8) }}

- A. print(b[3]"是"+c[3]+"成立"+str(a[5])+"周年｡")
- B. print(b[3]"是"+c[:3]+"成立"+a[5]+"周年｡")
- C. print(b[3]+"是"+c[:4]+"成立"+str(a(5))+"周年｡")
- D. print(str(b[3])+"是"+c[:4]+"成立"+str(a[5])+"周年｡")

---

9. 以下程序的作用是？

```python
for i in range(2024,2124):
    if(i%400==0 or (i%4==0 and i%100!=0)):
        print("{}是闰年｡".format(i))
    else:
        continue
```

{{ select(9) }}

- A. 从2024遍历到2124，判断哪些年份是闰年，并输出闰年年份
- B. 从2024遍历到2123，判断哪些年份是闰年，并输出闰年年份
- C. 从2025遍历到2124，判断哪些年份是闰年，并输出闰年年份
- D. 寻找2024年至2123年期间的第一个闰年

---

10. 在Python中，下列选项不属于循环结构语句的是？

{{ select(10) }}

- A. `for 变量 in 序列:`
    语句块
- B. `while(判断条件):`
    语句块
- C. `for 变量 in range(100):`
    语句块
- D. `do:`
    语句块
    `while(判断条件);`

---

11. 以下选项无法创建列表的是？

{{ select(11) }}

- A. ls=listt()
- B. ls=['字典集合']
- C. ls=['列表','元组','字符串']
- D. ls=list(range(10))

---

12. 以下程序段执行的结果是？

```python
ls=[1,2]
ls.append([1,2,3])
ls.extend([1,2])
ls.remove(2)
print(ls)
```

{{ select(12) }}

- A. [1,[1,2,3],[1,2]]
- B. [1, [1,2,3], 1, 2]
- C. [1,[1,3],[]]
- D. [1,1,3,1]

---

13. 阿宝的成绩字典`score={'数学':100,'语文':95,'英语':98,'科学':97,'美术':99}`，以下删除"音乐"成绩的语句正确的是？

{{ select(13) }}

- A. del score[音乐]
- B. del score['音乐']
- C. score.pop(音乐)
- D. score.remove('音乐')

---

14. 下面哪个选项是格式正确的元组？

{{ select(14) }}

- A. [1,2,3]
- B. {1,2,3}
- C. (1,2,3)
- D. (1｡2｡3)

---

15. 运行以下代码，终端会打印什么内容？

```python
for i in range(10):
    if i>5:
        print(i, end="")
```

{{ select(15) }}

- A. 12345678910
- B. 0123456789
- C. 56789
- D. 6789

---

16. 将古诗《春晓》句子存入列表`lst=['春眠不觉晓,','处处闻啼鸟,','夜来风雨声,','花落知多少｡']`，则`len(lst[3])`的值是？

{{ select(16) }}

- A. 4
- B. 5
- C. 6
- D. 7

---

17. 数学成绩元组`tuple=(88,93,76,85,90)`，打印第二个分数的代码是？

{{ select(17) }}

- A. print(tuple[1])
- B. print(tuple.get(1))
- C. print(tuple[2])
- D. print(tuple.index(1))

---

18. 小明浇花：土壤湿度`moisture`低于30%则浇水，下列语句正确的是？

{{ select(18) }}

- A. if moisture>30%:
    print('浇水')
- B. if moisture<30:
    print('浇水')
- C. if moisture>30:
    print('不浇水')
- D. if moisture<30:
    print('不浇水')

---

19. 判断数字`num`是否为偶数，正确的语句是？

{{ select(19) }}

- A. if num%2 !=0:
    print("是偶数")
- B. if num%2==0:
    print("是偶数")
- C. if num/2==0:
    print("是偶数")
- D. if num/2!=0:
    print("是偶数")

---

20. 以下各项中，表示列表的是？

{{ select(20) }}

- A. [1;3;3;2;4]
- B. {1,3,3,2,4}
- C. [1,3,3,2,4]
- D. (1,3,3,2,4)

---

21. 以下关于字符串的描述，不正确的是？

{{ select(21) }}

- A. 字符串定义后不可修改
- B. 字符串可以用"*"操作符进行连接
- C. 字符串是用单引号或双引号括起来的一串字符
- D. 内置函数len()返回字符串的长度

---

22. 已知`dict={'语文':59,'数学':99,'英语':97}`，执行以下哪个命令可修改为`{'语文':95,'数学':99,'英语':97}`？

{{ select(22) }}

- A. dict[0]=95
- B. dict[1]=95
- C. dict[语文]=95
- D. dict['语文']=95

---

23. 创建元组时，使用下列哪个符号？

{{ select(23) }}

- A. ()
- B. []
- C. {}
- D. <>

---

24. 运行以下程序，打印的最后一个数字是？

```python
i=0
while i<100:
    print(i)
    i=i+1
```

{{ select(24) }}

- A. 98
- B. 99
- C. 100
- D. 101

---

25. 在Python中，下列关于`range()`说法正确的是？

{{ select(25) }}

- A. range()是列表
- B. range()是函数，能生成一系列英文字母
- C. range()是函数，能生成一系列数字
- D. range()是函数，能生成一系列汉字

---

## 二、判断题（每题2分，共20分）

26. for、while和if均可以相互嵌套使用。

{{ select(26) }}

- A. 正确
- B. 错误

---

27. `str1='加油!'+'加油!'+'加油!'`

`str2='加油!'*3`

`str1和str2是一样的，都是"加油!加油!加油!"`

{{ select(27) }}

- A. 正确
- B. 错误

---

28. 在编写"输入整数并判断奇偶性"的程序时，可使用`input()`获取输入，并用`int()`转换为整数类型。

{{ select(28) }}

- A. 正确
- B. 错误

---

29. 执行代码`print("今年是%d年，农历%s年｡"%(2024,甲辰))`，得到的输出结果为"今年是2024年，农历甲辰年｡"。

{{ select(29) }}

- A. 正确
- B. 错误

---

30. 可以使用`index(x)`方法获取所有值为x的元素的索引。

{{ select(30) }}

- A. 正确
- B. 错误

---

31. break语句可以跳出所有循环。

{{ select(31) }}

- A. 正确
- B. 错误

---

32. 在`if...else...`语句中，当if语句不执行时，会执行else语句。

{{ select(32) }}

- A. 正确
- B. 错误

---

33. `del`语句可以删除整个列表，`clear()`方法可以清除列表中所有的元素。

{{ select(33) }}

- A. 正确
- B. 错误

---

34. 字典（dictionary）和列表（list）的共同点是都可以通过下标访问内容。

{{ select(34) }}

- A. 正确
- B. 错误

---

35. 在Python中，元组是无序不重复的。

{{ select(35) }}

- A. 正确
- B. 错误

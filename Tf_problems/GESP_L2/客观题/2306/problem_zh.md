# 2023年6月 C++二级考试真题

## 一、单项选择题（每题2分，共30分）

1. 高级语言编写的程序需要经过以下( )操作，可以生成在计算机上运行的可执行代码。

{{ select(1) }}

- A. 编辑
- B. 保存
- C. 调试
- D. 编译

---

2. 能够实现流程图"条件判断为假时执行语句块"功能的伪代码是( )。

{{ select(2) }}

- A. if 条件判断 then 语句块
- B. if 条件判断 then 什么也不做 else 语句块
- C. while 条件判断 do 语句块
- D. while not 条件判断 do 语句块

---

3. 下列关于C++语言的叙述，正确的是( )。

{{ select(3) }}

- A. char类型变量不能赋值给int类型的变量
- B. 两个int类型变量相乘，计算结果还是int类型
- C. 计算两个int类型变量相乘时，如果乘积超出int取值范围，程序会报错崩溃
- D. 计算两个double类型变量相除时，如果除数为0.0，程序会报错崩溃

---

4. 下列关于C++语言的叙述，不正确的是( )。

{{ select(4) }}

- A. if语句中的判断条件必须用小括号'('和')'括起来
- B. for语句中两个';'之间的循环条件可以省略，表示循环条件一直满足
- C. 循环体包含多条语句时，可以用缩进消除二义性
- D. 除了"先乘除、后加减"，还有很多运算符优先级

---

5. 以下哪个是C++语言的关键字?

{{ select(5) }}

- A. main
- B. max
- C. double
- D. sqrt

---

6. 以下哪个不是C++语言的运算符?

{{ select(6) }}

- A. >=
- B. /=
- C. ||
- D. <>

---

7. 如果a为int类型的变量，b为char类型的变量，则下列哪个语句不符合C++语法?

{{ select(7) }}

- A. a = a + 1.0;
- B. a = (int)(b - '0');
- C. b = (char)(a + '0');
- D. (int)b = a;

---

8. 用int变量a、b表示平行四边形两条边长，h表示a边对应的高，下列哪个表达式不能计算b边对应的高?

{{ select(8) }}

- A. a / b * (0.0 + h)
- B. (0.0 + a * h) / b
- C. a * h / (b + 0.0)
- D. (a + 0.0) * h / b

---

9. 以下哪个循环语句会无限次执行?

{{ select(9) }}

- A. for (int a = 0; a; a++) ;
- B. for (bool b = false; b <= true; b++) ;
- C. for (char c = 'A'; c < 'z'; c++) ;
- D. for (double d = 0.0; d < 10.0; d += 0.001) ;

---

10. 如果a为char类型变量，值为'C'（ASCII码67），执行cout << (a + 2)会输出( )。

{{ select(10) }}

- A. E
- B. C+2
- C. C2
- D. 69

---

11. 如果a和b均为int类型变量，下列表达式能正确判断"a等于1且b等于1"的是( )。

{{ select(11) }}

- A. (a == b) && (b == 1)
- B. (a && b)
- C. (a == b == 1)
- D. (a * b == 1)

---

12. 如果a为char类型变量，下列哪个表达式可以正确判断"a是数字"?

{{ select(12) }}

- A. '0' <= a && a <= '9'
- B. '1' <= a && a <= '0'
- C. '0' <= a <= '9'
- D. '1' <= a <= '0'

---

13. 在代码横线处填写( )，使得输出是9。

```cpp
#include<iostream>
using namespace std;
int main(){
    char a = '3', b = '6';
    cout << _;//横线处
    return 0;
}
```

{{ select(13) }}

- A. (a + b)
- B. (a + b - '0')
- C. (char)(a + b)
- D. (char)(a + b - '0')

---

14. 在代码横线处填写( )，使得输出是42。

```cpp
#include<iostream>
using namespace std;
int main(){
    int sum = 0;
    for (int i = 1; i <= 20; i++)
        if (_)//横线处
            sum += i;
    cout << sum << endl;
    return 0;
}
```

{{ select(14) }}

- A. i % 3 == 0
- B. 20 % i == 0
- C. i <= 8
- D. i >= 18

---

15. 执行以下C++程序后，输出结果是( )。

```cpp
#include<iostream>
using namespace std;
int main(){
    for (char x = 'A'; x <= 'D'; x++)
        if ((x == 'A') + (x == 'C') + (x == 'D') + (x == 'B') == 3)
            cout << x;
    return 0;
}
```

{{ select(15) }}

- A. A
- B. B
- C. C
- D. D

---

## 二、判断题（每题2分，共20分）

16. 中华学习机CEC-I内存64KB，GB2312汉字字符集（6763个汉字，每个2字节）放入内存仅占用不超过1/5的内存空间。

{{ select(16) }}

- A. 正确
- B. 错误

---

17. 域名gesp.ccf.org.cn的顶级域名是gesp。

{{ select(17) }}

- A. 正确
- B. 错误

---

18. C++中不能直接使用sqrt、abs等数学函数，包含<cmath>或<math.h>头文件后可使用。

{{ select(18) }}

- A. 正确
- B. 错误

---

19. C++标识符中可以有下划线'_'，且'_'是C++的运算符。

{{ select(19) }}

- A. 正确
- B. 错误

---

20. 如果a是double类型变量（值为3.5），则表达式a * 10的计算结果为35，且结果类型为int。

{{ select(20) }}

- A. 正确
- B. 错误

---

21. 在if语句中，条件成立时需执行多条语句，可使用大括号'{'和'}'将这些语句括起来。

{{ select(21) }}

- A. 正确
- B. 错误

---

22. 循环语句的循环体有可能无限制地执行下去。

{{ select(22) }}

- A. 正确
- B. 错误

---

23. ++和==都是C++的运算符，但+=不是。

{{ select(23) }}

- A. 正确
- B. 错误

---

24. 如果a为char类型变量（取值为大写字母'F'），执行语句a = a + 1;后，a的值会变为大写字母'G'。

{{ select(24) }}

- A. 正确
- B. 错误

---

25. 表达式sqrt(9.0)的计算结果为3，且结果类型为int。

{{ select(25) }}

- A. 正确
- B. 错误

---

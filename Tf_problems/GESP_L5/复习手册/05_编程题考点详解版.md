# GESP 五级核心考点分析与 8 小时集训计划

> 基于 2023.9-2025.12 共 20 套编程真题深度分析
>
> **适用对象**：GESP C++ 五级考生
> **目标分数**：90 分+
> **编译器标准**：C++11（考试指定标准）
> 
> **注意**：本文档所有代码均已适配 C++11 标准，不使用 C++17 及更高版本特性（如结构化绑定 `auto [a, b]`）。

---

## 📊 考点分布统计

| 考点类别 | 出现频次 | 占比 | 难度系数 |
|---------|---------|------|---------|
| 排序算法 | 6 | 30% | ⭐⭐⭐ |
| 贪心算法 | 5 | 25% | ⭐⭐⭐ |
| 二分查找 | 4 | 20% | ⭐⭐⭐ |
| 素数与因数分解 | 4 | 20% | ⭐⭐ |
| 筛法 | 3 | 15% | ⭐⭐ |
| 数学计算 | 5 | 25% | ⭐⭐ |
| 数位处理 | 2 | 10% | ⭐⭐⭐ |

---

## 🔥 核心考点详解

### 1️⃣ 多关键字排序（**最高频考点**）

**题型特点**：按多个条件依次排序，处理并列排名

| 题目 | 排序规则 | 核心技巧 |
|-----|---------|---------|
| 成绩排序 | 总分 → 语文+数学 → 语文/数学最高分 | tuple 逆序排序 |
| 学生排名 | 平均分 → 数学 → 语文 | 自定义比较函数 |
| 数据整理 | 多关键字升序/降序 | stable_sort 稳定性 |

**模板代码**：

```cpp
// 方法1：使用 tuple（C++11）
struct Student {
    int id;
    int total;
    int cm;
    int max_cm;
};

bool cmp(const Student& a, const Student& b) {
    if (a.total != b.total) return a.total > b.total;  // 总分降序
    if (a.cm != b.cm) return a.cm > b.cm;              // 语文+数学降序
    return a.max_cm > b.max_cm;                        // 最高分降序
}
```

**并列排名处理**：

```cpp
// 排名并列处理
vector<int> rank(N, 1);
for (int i = 1; i < N; i++) {
    if (students[i].total == students[i-1].total &&
        students[i].cm == students[i-1].cm) {
        rank[i] = rank[i-1];
    } else {
        rank[i] = i + 1;
    }
}
```

**易错点**：
- 降序与升序搞混
- 并列时排名重复（如 1, 1, 3）
- stable_sort 与 sort 区别

---

### 2️⃣ 贪心算法（**必考题型**）

**题型特点**：在每一步选择当前最优，期望得到全局最优

| 题目 | 贪心策略 | 正确性说明 |
|-----|---------|----------|
| 巧夺大奖 | 优先选奖励最高，安排在最晚可用时段 | 倒序填坑法 |
| 奖品兑换 | 二分 + 贪心验证 | 检查是否可兑换 |
| 数字移动 | 优先移动体力大的 | 最大值优先 |
| 任务调度 | 奖励降序 + deadline 填坑 | 贪心选择 |

**经典贪心问题模板**：

```cpp
// 活动选择/任务调度
struct Task {
    int deadline;
    int reward;
};

bool taskCmp(const Task& a, const Task& b) {
    return a.reward > b.reward;  // 奖励降序
}

int maxReward(vector<Task>& tasks, int n) {
    sort(tasks.begin(), tasks.end(), taskCmp);
    vector<bool> used(n + 1, false);
    int total = 0;
    for (size_t i = 0; i < tasks.size(); i++) {
        const Task& t = tasks[i];
        // 找最晚可用时段
        for (int d = min(t.deadline, n); d >= 1; d--) {
            if (!used[d]) {
                used[d] = true;
                total += t.reward;
                break;
            }
        }
    }
    return total;
}
```

**贪心 vs 动态规划**：

| 特征 | 贪心算法 | 动态规划 |
|------|---------|---------|
| 选择方式 | 只考虑当前最优 | 考虑所有情况 |
| 最优解 | 局部最优 | 全局最优 |
| 适用问题 | 贪心选择性质 | 最优子结构 |
| 时间复杂度 | 通常 O(nlogn) | 可能 O(n²) |

**易错点**：
- 贪心不一定得到最优解（如硬币找零）
- 需要验证贪心策略的正确性
- 活动调度的 deadline 理解

---

### 3️⃣ 二分查找（**高频考点**）

**题型特点**：在有序数据中快速查找，或求最优解

| 题目 | 应用场景 | 二分类型 |
|-----|---------|---------|
| 奖品兑换 | 求最大可兑换份数 | 二分答案 |
| 数字移动 | 求最小 X | 二分答案 |
| 切割木头 | 求最小切割长度 | 二分答案 |

**二分答案模板**：

```cpp
// 二分求最优解
long long check(long long mid, ...) {
    // 判定 mid 是否可行
    // 返回 true/false
}

long long binarySearch(long long lo, long long hi) {
    while (lo < hi) {
        long long mid = lo + (hi - lo) / 2;
        if (check(mid, ...)) {
            hi = mid;  // 可行，找更小的
        } else {
            lo = mid + 1;  // 不可行，找更大的
        }
    }
    return lo;
}
```

**二分查找变体**：

```cpp
// 查找第一个 >= x 的位置
int lower_bound(vector<int>& arr, int x) {
    int l = 0, r = arr.size();
    while (l < r) {
        int mid = l + (r - l) / 2;
        if (arr[mid] >= x) r = mid;
        else l = mid + 1;
    }
    return l;
}

// 查找最后一个 <= x 的位置
int upper_bound(vector<int>& arr, int x) {
    int l = 0, r = arr.size();
    while (l < r) {
        int mid = l + (r - l + 1) / 2;
        if (arr[mid] <= x) l = mid;
        else r = mid - 1;
    }
    return l;
}
```

**易错点**：
- 循环条件（lo < hi vs lo <= hi）
- mid 计算防溢出
- 判定函数逻辑正确性

---

### 4️⃣ 素数与因数分解（**核心考点**）

**题型特点**：分解整数的质因数，进行计算或判断

| 题目 | 核心操作 | 方法 |
|-----|---------|-----|
| 因数分解 | N = p1^e1 × p2^e2 × ... | 试除法 |
| 奇妙数字 | 指数求和 | 质因数分解 |
| 数字选取 | 找质数个数 | 筛法 |
| 小杨的幸运数 | 筛法标记 | 埃氏筛 |

**质因数分解模板**：

```cpp
vector<pair<long long, int>> factorize(long long n) {
    vector<pair<long long, int>> factors;
    for (long long p = 2; p * p <= n; p++) {
        if (n % p == 0) {
            int cnt = 0;
            while (n % p == 0) {
                n /= p;
                cnt++;
            }
            factors.push_back({p, cnt});
        }
    }
    if (n > 1) factors.push_back({n, 1});
    return factors;
}
```

**大数质因数分解优化**：

```cpp
// 处理到 10^12 的质因数分解
vector<pair<long long, int>> factorize_big(long long n) {
    vector<pair<long long, int>> factors;
    // 先处理 2
    if (n % 2 == 0) {
        int cnt = 0;
        while (n % 2 == 0) {
            n /= 2;
            cnt++;
        }
        factors.push_back({2, cnt});
    }
    // 处理奇数
    for (long long p = 3; p * p <= n; p += 2) {
        if (n % p == 0) {
            int cnt = 0;
            while (n % p == 0) {
                n /= p;
                cnt++;
            }
            factors.push_back({p, cnt});
        }
    }
    if (n > 1) factors.push_back({n, 1});
    return factors;
}
```

**易错点**：
- 循环边界 p * p <= n
- n 处理完后 >1 的情况
- 指数合并输出格式

---

### 5️⃣ 筛法（**高效算法**）

**题型特点**：快速找出范围内所有素数

| 筛法类型 | 时间复杂度 | 适用场景 |
|---------|-----------|---------|
| 埃氏筛 | O(NloglogN) | N ≤ 10⁷ |
| 线性筛 | O(N) | N ≤ 10⁷+ |
| 试除法 | O(√N) | 单个素数判定 |

**埃氏筛模板**：

```cpp
vector<bool> sieve(int n) {
    vector<bool> is_prime(n + 1, true);
    is_prime[0] = is_prime[1] = false;
    for (int i = 2; i * i <= n; i++) {
        if (is_prime[i]) {
            for (int j = i * i; j <= n; j += i) {
                is_prime[j] = false;
            }
        }
    }
    return is_prime;
}
```

**线性筛模板**：

```cpp
vector<int> linearSieve(int n) {
    vector<bool> is_prime(n + 1, true);
    vector<int> primes;
    for (int i = 2; i <= n; i++) {
        if (is_prime[i]) primes.push_back(i);
        for (int p : primes) {
            if (1LL * i * p > n) break;
            is_prime[i * p] = false;
            if (i % p == 0) break;
        }
    }
    return primes;
}
```

**筛法应用**：

```cpp
// 小杨的幸运数：标记完全平方数和其倍数
int main() {
    int a;
    cin >> a;
    const int MAX = 100000;
    vector<bool> lucky(MAX + 1, false);
    
    // 标记超级幸运数（完全平方数）
    for (int i = sqrt(a); i * i <= MAX; i++) {
        int sq = i * i;
        if (sq >= a) {
            // 标记所有倍数
            for (int j = sq; j <= MAX; j += sq) {
                lucky[j] = true;
            }
        }
    }
}
```

**易错点**：
- 埃氏筛从 i*i 开始
- 线性筛 break 条件
- 内存占用问题

---

### 6️⃣ 二进制数位处理（**较难考点**）

**题型特点**：处理二进制表示中 1 的个数

| 题目 | 要求 | 解法 |
|-----|-----|-----|
| 有趣的数字和 | 统计 1 的个数为奇数的数之和 | 数位 DP / 规律 |
| 二进制统计 | 区间 [l, r] 内满足条件的数 | 递归/迭代 |

**数位 DP 模板**：

```cpp
// 统计 [0, n] 中二进制 1 的个数为奇数的数之和
long long solve(long long n) {
    // 思路：找规律
    // 每 4 个数形成一个周期：0, 1, 2, 3
    // 0(00): 偶数个1
    // 1(01): 奇数个1
    // 2(10): 奇数个1
    // 3(11): 偶数个1
    
    // 前 2^k 个数中，奇数个1的数有 2^(k-1) 个
    // ...
}
```

**更简洁的方法**：

```cpp
// 规律：每 4 个数中，位置 1 和 2 的数有奇数个 1
long long countOddOnes(long long n) {
    long long count = 0;
    // 考虑每一位的贡献
    for (long long i = 0; (1LL << i) <= n; i++) {
        long long block = 1LL << (i + 1);
        long long fullBlocks = n / block;
        count += fullBlocks * (block / 4) * 2;
        long long remainder = n % block;
        long long extra = max(0LL, remainder - (block / 2) + 1);
        count += extra;
    }
    return count;
}
```

**易错点**：
- 二进制与十进制的转换
- 1 的个数计算（__builtin_popcount）
- 区间端点的处理

---

### 7️⃣ 数学计算与优化（**综合考点**）

**题型特点**：结合数学公式进行优化计算

| 题目 | 数学知识点 | 解题关键 |
|-----|----------|---------|
| 相等序列 | 质因数分解 + 中位数 | 各因子指数独立优化 |
| 奇妙数字 | 指数求和 | n 的质因数分解 |
| 完全数判断 | 约数和公式 | 枚举到 √n |

**中位数优化**：

```cpp
// 相等序列：将所有数变为相同值的最小花费
long long minCost(vector<long long>& nums) {
    // 对每个质因子，求指数的中位数
    // 总花费 = sum(|ai - median|)
    
    // 1. 分解所有数的质因数
    map<long long, vector<int> > prime_exponents;
    for (size_t i = 0; i < nums.size(); i++) {
        long long num = nums[i];
        vector<pair<long long, int> > factors = factorize(num);
        for (size_t j = 0; j < factors.size(); j++) {
            prime_exponents[factors[j].first].push_back(factors[j].second);
        }
    }
    
    // 2. 对每个质因子，求中位数并计算花费
    long long total = 0;
    for (map<long long, vector<int> >::iterator it = prime_exponents.begin();
         it != prime_exponents.end(); it++) {
        vector<int>& exps = it->second;
        sort(exps.begin(), exps.end());
        int median = exps[exps.size() / 2];
        for (size_t k = 0; k < exps.size(); k++) {
            total += abs(exps[k] - median);
        }
    }
    return total;
}
```

**完全数判断**：

```cpp
bool isPerfectNumber(long long n) {
    if (n <= 1) return false;
    long long sum = 1;
    for (long long i = 2; i * i <= n; i++) {
        if (n % i == 0) {
            sum += i;
            if (i != n / i) sum += n / i;
        }
    }
    return sum == n;
}
```

---

## 📈 难度分布

| 难度 | 题目特征 | 占比 | 代表题目 |
|-----|---------|------|---------|
| ⭐ 基础 | 单排序/简单计算 | 20% | 因数分解 |
| ⭐⭐ 中等 | 贪心+验证/筛法 | 45% | 巧夺大奖 |
| ⭐⭐⭐ 较难 | 二分+贪心/数位处理 | 35% | 有趣的数字和 |

---

## 🎯 备考建议

### 必须掌握的核心技能

1. **排序算法**（必须精通）
   - sort 自定义比较函数
   - 多关键字排序
   - stable_sort 稳定性

2. **二分查找**（高频考点）
   - 二分答案
   - 判定函数设计
   - 边界条件处理

3. **贪心算法**（必考题型）
   - 活动选择问题
   - 倒序填坑法
   - 贪心正确性验证

4. **数学运算**
   - 质因数分解
   - 筛法求素数
   - 中位数优化

### 易错点提醒

| 易错点 | 示例 | 正确做法 |
|-------|-----|---------|
| 降序排序 | a > b 写反 | 与题意对照检查 |
| deadline 理解 | 理解成开始时间 | 理解为截止时间 |
| 二分边界 | lo < hi vs lo <= hi | 多写几组数据验证 |
| 中位数位置 | 奇偶个数处理 | size/2 是下中位数 |
| 筛法内存 | 10^8 级别 | 控制 N 的大小 |

---

# 8 小时集训计划

## 📋 总体时间安排

| 时段 | 时长 | 内容模块 | 真题训练 |
|------|------|---------|---------|
| 0:00-0:45 | 45分钟 | 基础巩固：排序与比较函数 | 1-2道排序题 |
| 0:45-1:30 | 45分钟 | 核心考点1：贪心算法 | 2-3道贪心题 |
| 1:30-2:15 | 45分钟 | 核心考点2：二分查找 | 2道二分题 |
| 2:15-3:00 | 45分钟 | 核心考点3：筛法与质因数 | 2道筛法题 |
| 3:00-3:45 | 45分钟 | 核心考点4：数学优化 | 2道数学题 |
| 3:45-4:30 | 45分钟 | 综合演练：混合考点 | 2道综合题 |
| 4:30-5:30 | 60分钟 | 综合模拟：2套真题限时训练 | 实战演练 |
| 5:30-6:00 | 30分钟 | 错题讲解与答疑 | 针对性补强 |
| 6:00-6:30 | 30分钟 | 技巧总结与考前提醒 | 心理建设 |

---

## 📖 详细培训计划

### ⏰ 第一时段：基础巩固（0:00-0:45）

#### 教学内容

**1. C++ 排序回顾（15分钟）**

```cpp
// sort 自定义比较函数
bool descCmp(int a, int b) {
    return a > b;  // 降序
}
sort(arr.begin(), arr.end(), descCmp);

// 多关键字排序（使用结构体）
struct Student {
    int score;
    int math;
    int chinese;
};

bool multiCmp(const Student& a, const Student& b) {
    if (a.score != b.score) return a.score > b.score;
    if (a.math != b.math) return a.math > b.math;
    return a.chinese > b.chinese;
}
sort(students.begin(), students.end(), multiCmp);

// stable_sort 保持相等元素的相对顺序
stable_sort(arr.begin(), arr.end(), cmp);
```

**2. 贪心基础（15分钟）**

```cpp
// 活动选择：按截止时间排序
bool deadlineCmp(const Task& a, const Task& b) {
    return a.deadline < b.deadline;
}
sort(tasks.begin(), tasks.end(), deadlineCmp);

// 奖励优先：按奖励降序
bool rewardCmp(const Task& a, const Task& b) {
    return a.reward > b.reward;
}
sort(tasks.begin(), tasks.end(), rewardCmp);
```

**3. 二分基础（15分钟）**

```cpp
// 二分答案框架
long long check(long long mid) {
    // 实现判定逻辑
    return 可行 / 不可行;
}

long long binarySearch(long long lo, long long hi) {
    while (lo < hi) {
        long long mid = lo + (hi - lo) / 2;
        if (check(mid)) hi = mid;
        else lo = mid + 1;
    }
    return lo;
}
```

#### 实战练习（15分钟）

**题目：成绩排序（240301）**
- 输入 N 学生及其成绩
- 按总分 → 语文+数学 → 单科最高 排序
- 时间：15分钟

---

### ⏰ 第二时段：贪心算法（0:45-1:30）

#### 核心技能：倒序填坑法（20分钟）

**模板代码**：

```cpp
int greedyTask(vector<int>& deadlines, vector<int>& rewards) {
    // 1. 按奖励降序
    vector<pair<int, int> > tasks;
    for (int i = 0; i < n; i++) {
        tasks.push_back(make_pair(rewards[i], deadlines[i]));
    }
    
    bool pairCmp(const pair<int, int>& a, const pair<int, int>& b) {
        return a.first > b.first;  // 按奖励（first）降序
    }
    sort(tasks.begin(), tasks.end(), pairCmp);
    
    // 2. 倒序填坑
    vector<bool> used(n + 1, false);
    int total = 0;
    for (size_t i = 0; i < tasks.size(); i++) {
        const pair<int, int>& task = tasks[i];
        int reward = task.first;
        int deadline = task.second;
        for (int d = min(deadline, n); d >= 1; d--) {
            if (!used[d]) {
                used[d] = true;
                total += reward;
                break;
            }
        }
    }
    return total;
}
```

#### 真题训练（25分钟）

**题目1：巧夺大奖（230902）**
```
每个小游戏有截止时间和奖励
每个时间段只能完成一个游戏
求最大奖励
训练目标：贪心 + 倒序填坑
难度：⭐⭐⭐
时间：12分钟
```

**题目2：奖品兑换（250601）**
```
n张课堂券，m张作业券
兑换方式：a张课堂+b张作业 或 b张课堂+a张作业
求最多可兑换份数
训练目标：二分答案 + 贪心验证
难度：⭐⭐⭐
时间：13分钟
```

#### 技巧总结（5分钟）

| 技巧 | 说明 |
|-----|------|
| 奖励降序 | 优先完成奖励高的任务 |
| 倒序查找 | 从截止日期往前找空位 |
| 验证贪心 | 先写验证函数再二分 |

---

### ⏰ 第三时段：二分查找（1:30-2:15）

#### 核心技能：二分答案（20分钟）

**模板代码**：

```cpp
// 判定函数设计
bool canDo(long long x, ...) {
    // x 是候选答案
    // 返回 x 是否可行
    // 例如：x 份奖品是否可兑换
    // 例如：移动花费不超过 x 是否可行
}

// 二分主逻辑
long long solve() {
    long long lo = 0, hi = 上限;
    while (lo < hi) {
        long long mid = lo + (hi - lo) / 2;
        if (canDo(mid, ...)) {
            hi = mid;  // 可行，找更小的
        } else {
            lo = mid + 1;  // 不可行，找更大的
        }
    }
    return lo;
}
```

#### 真题训练（25分钟）

**题目1：数字移动（251201）**
```
n个整数，恰好n/2对相同数字
每次移动代价为数字大小
求最小的最大移动代价X
训练目标：二分 + 贪心验证
难度：⭐⭐⭐
时间：12分钟
```

**题目2：切割木头（扩展）**
```
长度为L的木头，切K次
每次切割长度为x，得到 (L-1)/x + 1 段
求最小x使得段数 >= K
训练目标：二分 + 数学验证
难度：⭐⭐⭐
时间：13分钟
```

#### 技巧总结（5分钟）

| 技巧 | 说明 |
|-----|------|
| 确定上下界 | 上界为最大值，下界为0 |
| 判定函数 | 先实现判定，再写二分 |
| 结果检查 | 输出 lo 或 hi 需确认 |

---

### ⏰ 第四时段：筛法与质因数（2:15-3:00）

#### 核心技能（25分钟）

**1. 质因数分解**：

```cpp
vector<pair<long long, int>> factorize(long long n) {
    vector<pair<long long, int>> res;
    for (long long p = 2; p * p <= n; p++) {
        if (n % p == 0) {
            int cnt = 0;
            while (n % p == 0) {
                n /= p;
                cnt++;
            }
            res.push_back({p, cnt});
        }
    }
    if (n > 1) res.push_back({n, 1});
    return res;
}
```

**2. 埃氏筛**：

```cpp
vector<int> sieve(int n) {
    vector<bool> is_prime(n + 1, true);
    is_prime[0] = is_prime[1] = false;
    for (int i = 2; i * i <= n; i++) {
        if (is_prime[i]) {
            for (int j = i * i; j <= n; j += i)
                is_prime[j] = false;
        }
    }
    return is_prime;
}
```

#### 真题训练（20分钟）

**题目1：因数分解（230901）**
```
输入 N (2 ≤ N ≤ 10^12)
输出 N 的质因数分解式
训练目标：大数分解 + 格式输出
难度：⭐⭐
时间：10分钟
```

**题目2：数字选取（250901）**
```
从 1~n 中选取最大互质集合
答案 = 质数个数 + 1
训练目标：筛法计数 + 互质理解
难度：⭐⭐
时间：10分钟
```

---

### ⏰ 第五时段：数学优化（3:00-3:45）

#### 核心技能（25分钟）

**中位数优化**：

```cpp
long long minCostToEqual(vector<long long>& nums) {
    // 对每个质因子独立优化
    map<long long, vector<int> > exps;
    for (size_t i = 0; i < nums.size(); i++) {
        long long x = nums[i];
        vector<pair<long long, int> > facs = factorize(x);
        for (size_t j = 0; j < facs.size(); j++) {
            exps[facs[j].first].push_back(facs[j].second);
        }
    }
    
    long long cost = 0;
    for (map<long long, vector<int> >::iterator it = exps.begin();
         it != exps.end(); it++) {
        vector<int>& v = it->second;
        sort(v.begin(), v.end());
        int median = v[v.size() / 2];
        for (size_t k = 0; k < v.size(); k++) {
            cost += abs(v[k] - median);
        }
    }
    return cost;
}
```

#### 真题训练（20分钟）

**题目1：相等序列（251202）**
```
N个数，通过乘/除质数使所有数相等
求最小总花费
训练目标：质因数分解 + 中位数优化
难度：⭐⭐⭐
时间：12分钟
```

**题目2：奇妙数字（241201）**
```
奇妙数字 = p^a (p为质数)
求最大个数使得乘积是n的因子
训练目标：质因数分解 + 指数求和
难度：⭐⭐
时间：8分钟
```

---

### ⏰ 第六时段：综合演练（3:45-4:30）

#### 综合模拟（45分钟）

**题目1：小杨的幸运数（231201）**
```
超级幸运数：≥a 的完全平方数
幸运数：超级幸运数的倍数
求最少"幸运化"次数
训练目标：筛法 + 动态规划
难度：⭐⭐⭐
时间：15分钟
```

**题目2：有趣的数字和（250902）**
```
二进制中1的个数为奇数的数之和
求 [l, r] 范围内满足条件的数之和
训练目标：数位DP 或 数学规律
难度：⭐⭐⭐
时间：15分钟
```

---

### ⏰ 第七时段：综合模拟（4:30-5:30）

#### 限时训练（60分钟）

**第一套：2024年3月五级真题（30分钟）**

| 题目 | 类型 | 建议时间 |
|-----|-----|---------|
| 成绩排序 | 多关键字排序 | 10分钟 |
| 切割木头 | 二分答案 | 10分钟 |
| 数字选取 | 筛法 | 10分钟 |

**第二套：2025年6月五级真题（30分钟）**

| 题目 | 类型 | 建议时间 |
|-----|-----|---------|
| 奖品兑换 | 二分+贪心 | 12分钟 |
| 相等序列 | 数学优化 | 12分钟 |
| 有趣的数字和 | 数位处理 | 6分钟 |

---

### ⏰ 第八时段：错题讲解（5:30-6:00）

#### 常见错误分析

| 错误类型 | 典型表现 | 解决方法 |
|---------|---------|---------|
| 排序方向 | 升序降序写反 | 检查cmp返回值 |
| deadline | 理解成开始时间 | 注意题意描述 |
| 二分边界 | lo/hi 更新错误 | 画图验证 |
| 质因数 | 循环条件错误 | p*p <= n |
| 中位数 | 奇偶位置错误 | size/2 |

#### 个性化补强

根据学生错误分布，针对性讲解高频错题。

---

### ⏰ 第九时段：总结与技巧（6:00-6:30）

#### 考试技巧

**1. 时间分配策略**

| 题目难度 | 建议时间 | 策略 |
|---------|---------|------|
| 简单题 | 8分钟 | 确保正确 |
| 中等题 | 12分钟 | 思考后编码 |
| 较难题 | 15分钟 | 先跳过 |

**2. 调试技巧**

```cpp
// 1. 先测试小数据
// 输入: n=3 或 n=5 验证逻辑

// 2. 打印中间结果
cout << "mid=" << mid << " check=" << check(mid) << endl;

// 3. 边界测试
// 输入: 1, 最小值, 最大值
```

**3. 得分策略**

```
✓ 暴力解法也能过（不必追求最优）
✓ 部分分数：提交代码看结果
✓ 注意输出格式：空格、换行
```

#### 知识导图

```
GESP 五级核心考点
├── 排序算法
│   ├── 多关键字排序
│   ├── stable_sort
│   └── 并列排名处理
├── 贪心算法
│   ├── 活动选择
│   ├── 倒序填坑
│   └── 二分验证
├── 二分查找
│   ├── 二分答案
│   ├── 判定函数
│   └── 边界条件
├── 素数筛法
│   ├── 埃氏筛
│   ├── 线性筛
│   └── 质因数分解
└── 数学优化
    ├── 中位数
    ├── 约数和
    └── 数位处理
```

#### 考前 checklist

- [ ] 排序比较函数写对了吗？（升序/降序）
- [ ] 二分边界条件对吗？（lo < hi）
- [ ] 贪心策略验证了吗？（反例测试）
- [ ] 质因数分解完整吗？（处理 n > 1）
- [ ] 输出格式正确吗？（换行、空格）
- [ ] 编译通过了吗？

---

## 📝 配套练习题清单

### 必练基础题（1-2星）

| 序号 | 题目 | 考点 | 难度 |
|-----|-----|-----|-----|
| 1 | 因数分解(230901) | 质因数分解 | ⭐⭐ |
| 2 | 奇妙数字(241201) | 质因数+指数 | ⭐⭐ |
| 3 | 数字选取(250901) | 筛法 | ⭐⭐ |

### 必练中等题（2-3星）

| 序号 | 题目 | 考点 | 难度 |
|-----|-----|-----|-----|
| 4 | 成绩排序(240301) | 多关键字排序 | ⭐⭐⭐ |
| 5 | 巧夺大奖(230902) | 贪心+倒序填坑 | ⭐⭐⭐ |
| 6 | 小杨的幸运数(231201) | 筛法+DP | ⭐⭐⭐ |

### 进阶挑战题（3-4星）

| 序号 | 题目 | 考点 | 难度 |
|-----|-----|-----|-----|
| 7 | 奖品兑换(250601) | 二分+贪心 | ⭐⭐⭐ |
| 8 | 数字移动(251201) | 二分+验证 | ⭐⭐⭐ |
| 9 | 相等序列(251202) | 数学优化 | ⭐⭐⭐ |
| 10 | 有趣的数字和(250902) | 数位DP | ⭐⭐⭐ |

---

## 🎯 培训效果评估

### 达标标准

| 题型 | 正确率要求 |
|-----|-----------|
| 排序算法 | ≥ 85% |
| 贪心算法 | ≥ 80% |
| 二分查找 | ≥ 80% |
| 筛法与质因数 | ≥ 85% |
| 综合模拟 | ≥ 75% |

### 补充练习建议

**未达标模块**：
- 排序算法：额外练习多关键字排序题
- 贪心算法：额外练习活动选择问题
- 二分查找：额外练习判定函数设计

---

## 📚 真题对应关系表

| 年份 | 套题 | 考点覆盖 |
|-----|-----|---------|
| 2023年9月 | 230901/230902 | 质因数分解、活动调度 |
| 2023年12月 | 231201/231202 | 筛法DP、贪心验证 |
| 2024年3月 | 240301/240302 | 多关键字排序、二分答案 |
| 2024年6月 | 240601/240602 | 数学计算、数位处理 |
| 2024年9月 | 240901/240902 | 筛法、贪心选择 |
| 2024年12月 | 241201/241202 | 质因数分解、数学优化 |
| 2025年3月 | 250301/250302 | 线性筛、原根判断 |
| 2025年6月 | 250601/250602 | 二分+贪心、数学优化 |
| 2025年9月 | 250901/250902 | 筛法、数位DP |
| 2025年12月 | 251201/251202 | 二分验证、中位数优化 |

---

> **注**：以上真题均可在 `./Tf_problems/GESP_L5/` 目录下找到对应文件夹，文件夹名称即为真题套题编号。

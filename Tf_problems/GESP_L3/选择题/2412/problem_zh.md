# 2024年12月 C++三级考试真题

## 一、单选题（每题2分，共30分）

1. 二进制`[10000011]原`和`[10000011]补`对应的十进制数值分别是( )

{{ select(1) }}

- A. -125,-3
- B. -3, -125
- C. -3,-3
- D. -125,-125

---

2. 关于计算机中的编码，下列说法错误的是( )

{{ select(2) }}

- A. 无符号数的原码就是真值
- B. 正数的反码是其本身
- C. 负数的反码和补码不一样
- D. 负数的反码是在原码基础上各个位取反

---

3. 16进制数B2025转换成8进制数是( )

{{ select(3) }}

- A. 2620045
- B. 2004526
- C. 729125
- D. 2420045

---

4. 625.625转换成二进制是( )｡

{{ select(4) }}

- A. 1001110001.101
- B. 101.101
- C. 101.1001110001
- D. 1001110001.1101

---

5. 下面逻辑运算中，正确的是( )

{{ select(5) }}

- A. 5&&3==0
- B. 5|3==8
- C. 5||3==11
- D. 5&3==0001

---

6. 补码11111101执行`11111101>>1`后得到的结果是( )

{{ select(6) }}

- A. 11111100
- B. -2
- C. 11111101
- D. 11111010

---

7. 下列代码的输出是( )

```cpp
#include<iostream>
#include<string>
using namespace std;
int main(){
    string str="12345"; string s="1234@@chenadai";
    s.replace(1,5,str);
    cout<<s<<endl;
    return 0;
}
```

{{ select(7) }}

- A. 12345
- B. 2345@
- C. 112345chenadai
- D. 12345chenadai

---

8. `a|10`（a与10均为十进制，二进制最高位为1）运算的结果是( )｡

{{ select(8) }}

- A. 使a的二进制从右往左第二位为1
- B. 使a的二进制从右往左第一位为0
- C. 使a的二进制从右往左第二位为0
- D. 使a的二进制最高位为0

---

9. 下列程序的输出是( )

```cpp
#include<iostream>
#include<string>
using namespace std;
int main(){
    string ch="hello";
    if(ch[5]==NULL)
        cout<<"right"<<endl;
    else if(ch[5]=='\0')
        cout<<"wrong"<<endl;
    else
        cout<<"hello"<<endl;
    return 0;
}
```

{{ select(9) }}

- A. right
- B. wrong
- C. hello
- D. 不能正确执行

---

10. 下列程序中，`char ch[] = "hello world";` 假设一个字符占1字节，ch占用的内存空间是( )

{{ select(10) }}

- A. 11
- B. 10
- C. 13
- D. 12

---

11. 下列程序最后输出的是( )

```cpp
#include<iostream>
#include<cctype>
using namespace std;
int main(){
    int a=65;
    cout<<tolower(a)<<endl;
    return 0;
}
```

{{ select(11) }}

- A. 65
- B. A
- C. a
- D. 97

---

12. 想要计算n到m之间（含n和m）数字d出现的次数，下列程序能实现的是( )｡

{{ select(12) }}

- A. 循环i从n+1开始，res未初始化
- B. if条件为`temp%10=d`（赋值错误），res未初始化
- C. 循环i从n到m，res初始为0，if条件正确
- D. 未定义temp变量

---

13. 兔子5元1只、鸡3元1只、鸭子1元3只，100元买100只（每种至少1只），下列程序能实现的是( )

{{ select(13) }}

- A. 条件为`25*i+10*j+k==300`（价格换算错误）
- B. 条件为`20*i+10*j+k==300`（价格换算错误）
- C. 循环范围错误，条件价格换算错误
- D. 条件为`15*i+9*j+k==300`（价格换算错误）

---

14. 求小于等于N的素数（遍历2到i-1判断能否整除），体现该方法的程序是( )

{{ select(14) }}

- A. 逻辑错误，flag判断颠倒
- B. 未输出2，flag重置错误
- C. 输出2，遍历3到N，能整除则flag=1，最终判断输出
- D. 无flag重置，逻辑不完整

---

15. 15天工作，15个金环项链，每天结算工资，最少剪裁成( )段

{{ select(15) }}

- A. 4段，分别是1、2、4、8
- B. 15段，每段1个
- C. 6段，分别是3、3、3、3、2、1
- D. 9段，分别是2、2、2、2、2、2、1、1、1

---

## 二、判断题（每题2分，共20分）

16. 补码的设计目的是简化运算电路，使加减法通过加法电路实现，将减正数/加负数转化为加正数｡( )

{{ select(16) }}

- A. 正确
- B. 错误

---

17. 使用原码计算2+(-1)的结果是-3｡( )

{{ select(17) }}

- A. 正确
- B. 错误

---

18. 反码计算加减法，结果都正确，仅解决不了-0的问题｡( )

{{ select(18) }}

- A. 正确
- B. 错误

---

19. 十进制63转换二进制，按"除2取余"顺序，结果是111111｡( )

{{ select(19) }}

- A. 正确
- B. 错误

---

20. 下列程序输出A：`char x=65; x=x&00001111; cout<<x<<endl;`

{{ select(20) }}

- A. 正确
- B. 错误

---

21. 下列程序最后pos的值是4：`string str="chenADai"; int pos = str.find('D'); --pos;`

{{ select(21) }}

- A. 正确
- B. 错误

---

22. 程序`string ch="chen"; cout<<ch[4]<<endl;`不能正确执行｡( )

{{ select(22) }}

- A. 正确
- B. 错误

---

23. 程序`char a='A'; a=a+32; cout<<(int)a<<endl;`输出97｡( )

{{ select(23) }}

- A. 正确
- B. 错误

---

24. 自然界中，最小的素数是2｡( )

{{ select(24) }}

- A. 正确
- B. 错误

---

25. 十六进制CCF = 七进制12363｡( )

{{ select(25) }}

- A. 正确
- B. 错误

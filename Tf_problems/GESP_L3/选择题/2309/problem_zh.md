# 2023年9月 C++三级考试真题

## 一、单选题（每题2分，共30分）

1. 人们所使用的手机上安装的App通常指的是( )｡

{{ select(1) }}

- A. 一款操作系统
- B. 一款应用软件
- C. 一种通话设备
- D. 以上都不对

---

2. 下列流程图的输出结果是？

开始→a=5,s=1→判断a≥4（是则s=s×a，a=a-1；否则输出s）→结束

{{ select(2) }}

- A. 60
- B. 20
- C. 5
- D. 1

---

3. 已知大写字符'A'的ASCII编码十六进制表示为0x41，则字符'L'的ASCII编码十六进制表示为( )｡

{{ select(3) }}

- A. 4A
- B. 4B
- C. 4C
- D. 52

---

4. 以下哪个不是C++语言中的运算符?( )

{{ select(4) }}

- A. ~
- B. ~~
- C. <
- D. <

---

5. 如果数组定义为`long long array[] = {3, 5, 7, 2};`，则数组array占用的字节数为( )｡

{{ select(5) }}

- A. 32
- B. 16
- C. 8
- D. 4

---

6. 一个数组定义为`double array[3];`，则可合理访问这个数组的元素的下标最大为( )｡

{{ select(6) }}

- A. 2
- B. 3
- C. 23
- D. 24

---

7. 以下数组定义，符合C++语言语法的是( )｡

{{ select(7) }}

- A. double a[];
- B. double b[] = {1, 2.0, '3'};
- C. double c[3.0];
- D. double[] d = new double[3];

---

8. 下列关于进制的叙述，正确的是( )｡

{{ select(8) }}

- A. 只有十进制和二进制能表示小数，八进制和十六进制不可以｡
- B. 常用进制包括二进制、八进制、十进制、十六进制，其他进制很少使用｡
- C. 对任意正整数，其二进制表示不会比十进制表示更短｡
- D. 正整数的八进制表示中，每一位可能出现的最大数字是8｡

---

9. 下列关于C++语言中数组的叙述，不正确的是( )｡

{{ select(9) }}

- A. 可以定义0个元素的数组｡
- B. 不能定义-1个元素的数组｡
- C. 数组下标越界访问会产生编译错误｡
- D. 运行时下标越界，程序仍可能正常结束｡

---

10. 如果a是int类型的变量，下列哪个表达式的值一定为true?( )

{{ select(10) }}

- A. a + 1000 - 1000 == a
- B. a * 2 / 2 == a
- C. (a & 1) == 1
- D. (a | 1) == a + 1

---

11. 如果a和b均为int类型的变量，下列表达式不能正确判断"a等于b"的是( )｡

{{ select(11) }}

- A. ((a >= b) && (a <= b))
- B. ((a >> 1) == (b >> 1))
- C. ((a + b) == (a + a))
- D. ((a ^ b) == 0)

---

12. 如果a为char类型的变量，下列哪个表达式可以正确判断"a是大写字母"?( )

{{ select(12) }}

- A. a - 'A' <= 26
- B. 'A' <= a <= 'Z'
- C. 'A' <= 'a' <= 'Z'
- D. ('A' <= a) && (a <= 'Z')

---

13. 在下列代码的横线处填写( )，可以使得输出是"20 10"｡

```cpp
#include<iostream>
using namespace std;
int main(){
    int a=10,b=20;
    a = (a << 8) | b;
    ____; // 横线处
    cout << a <<" "<<b << endl;
    return 0;
}
```

{{ select(13) }}

- A. a = a >> 8; b = a & 0xff
- B. b = a >> 8; a = a & 0xff;
- C. a = b; b = a & 0xff;
- D. b = a; a = b;

---

14. 在下列代码的横线处填写( )，可以使得输出是"120"｡

```cpp
#include<iostream>
using namespace std;
int main(){
    int array[5] = {1,2,3,4,5};
    int res=1;
    for (int i=0;i<5; i++)
        ____; // 横线处
    cout << res <<endl;
    return 0;
}
```

{{ select(14) }}

- A. res += array[i];
- B. res *= array[i];
- C. res = array[i];
- D. 以上均不对

---

15. 下列代码的输出是( )｡

```cpp
#include<iostream>
using namespace std;
int main(){
    int array[10];
    for(int i=0;i<10;i++) array[i] = i;
    for (int p=2;p<10; p++)
        if (array[p]==p)
            for (int n=p;n<10; n +=p)
                array[n] = array[n]/p*(p-1);
    int res=0;
    for (int n=1;n<10;n++) res += array[n];
    cout << res << endl;
    return 0;
}
```

{{ select(15) }}

- A. 15
- B. 28
- C. 45
- D. 55

---

## 二、判断题（每题2分，共20分）

16. 二进制数101.101在十进制下是5.005｡( )

{{ select(16) }}

- A. 正确
- B. 错误

---

17. 在C++中，位运算符有优先级规则，使用时应合理使用括号｡( )

{{ select(17) }}

- A. 正确
- B. 错误

---

18. 字符常量'3'的值和int类型常量3的值相同，仅占用字节数不同｡( )

{{ select(18) }}

- A. 正确
- B. 错误

---

19. 在C++中，长度为n的数组，访问下标为n的元素会引起编译错误｡( )

{{ select(19) }}

- A. 正确
- B. 错误

---

20. 在C++中，所有int类型的值经过若干次左移操作（<<）后，值总会变为0｡( )

{{ select(20) }}

- A. 正确
- B. 错误

---

21. 在C++中，数组下标的大小决定元素逻辑先后顺序，与内存中位置顺序无关｡( )

{{ select(21) }}

- A. 正确
- B. 错误

---

22. 在C++中，定义数组时，[]中必须指定元素个数｡( )

{{ select(22) }}

- A. 正确
- B. 错误

---

23. 哥德巴赫猜想可通过枚举法证明｡( )

{{ select(23) }}

- A. 正确
- B. 错误

---

24. 在C++中，表达式(0xff == 255)的值为true｡( )

{{ select(24) }}

- A. 正确
- B. 错误

---

25. 如果a为int类型变量，且表达式((a & 1) == 0)的值为true，则a是偶数｡( )

{{ select(25) }}

- A. 正确
- B. 错误

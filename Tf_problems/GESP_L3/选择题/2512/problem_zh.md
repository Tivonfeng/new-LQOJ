# 2025年12月 C++三级考试真题

## 一、单选题（每题2分，共30分）

1. 请将二进制数（1101.101）转换为等值的十进制数( )

{{ select(1) }}

- A. 14.25
- B. 13.625
- C. 13.5
- D. 14.5

---

2. 以下关于标准C++一维数组的描述，错误的是( )

{{ select(2) }}

- A. 数组的下标从0开始
- B. 数组在内存中是连续存放的
- C. 数组的长度只能在运行时才能确定
- D. 可以在程序中修改数组某个元素的值

---

3. 下列代码段执行后，变量sum的值是( )

```cpp
int sum = 0;
for (int i = 1; i <= 10; i++) {
    if (i % 3 == (8>>2)) sum += i;
}
```

{{ select(3) }}

- A. 12
- B. 17
- C. 33
- D. 40

---

4. 执行以下程序段，输出值是( )

```cpp
int x = 5;
if (x == (3 >> 2)) x = 8 >> 3;
cout << x << endl;
```

{{ select(4) }}

- A. 0
- B. 5
- C. 120
- D. 1

---

5. 已知字符串s = "C++ Programming"，下列程序运行的结果是( )

```cpp
cout << (int)(s.length() | 3) << endl;
```

{{ select(5) }}

- A. 14
- B. 15
- C. 16
- D. 17

---

6. 以下代码执行后，输出的结果是( )

```cpp
int arr[5] = {1, 2, 3, 4, 5};
int p = arr[1];
cout << (p + 2) << endl;
```

{{ select(6) }}

- A. 1
- B. 2
- C. 3
- D. 4

---

7. 以下关于C++求最小值函数min()的描述，不正确的是( )

{{ select(7) }}

- A. 函数必须返回一个值
- B. 该函数可以嵌套调用
- C. 调用min(5)是错误的，不能通过编译
- D. 调用min()（不传任何参数）可以通过编译，结果为0

---

8. 若unsigned int类型占用2个字节（每个字节8位），则其可表示的数据范围是( )

{{ select(8) }}

- A. 0 ~ 65535
- B. 0 ~ 65536
- C. -65536 ~ 65535
- D. 0 ~ 32767

---

9. 以下代码执行后，数组arr的内容是( )

```cpp
int arr[6] = {1, 2, 3, 4, 5, 6};
for (int i = 0; i < 6; i += 2) {
    arr[i] = arr[i] + arr[i+1];
    arr[i+1] = arr[i] - arr[i+1];
    arr[i] = arr[i] - arr[i+1];
}
```

{{ select(9) }}

- A. {2,1,4,3,6,5}
- B. {1,2,3,4,5,6}
- C. {3,1,4,2,5,3}
- D. {1,3,2,5,4,6}

---

10. 计算表达式a&b | (c^d)，其中a=3、b=7、c=15、d=4，结果是( )

{{ select(10) }}

- A. 十进制11
- B. 二进制11
- C. 八进制11
- D. 十六进制11

---

11. 整型变量x的初始值为10，以下代码的输出结果是( )

```cpp
int t = x--;
t -= x;
cout << t << endl;
```

{{ select(11) }}

- A. 0
- B. -1
- C. 1
- D. 死循环

---

12. 根据流程图，输入成绩score=60，输出结果是( )

```
开始→输入score→score≥90？（是输出"优秀"）→否→score>60？（是输出"及格"，否输出"不及格"）→结束
```

{{ select(12) }}

- A. 优秀
- B. 及格
- C. 不及格
- D. 没有输出

---

13. 以下代码运行后，sum的结果是( )

```cpp
int arr[5] = {2,4,6,8,10};
int sum = 0;
for (int i = 0; i < 5; i++) {
    switch (arr[i] % (1|2)) {
        case 0: sum +=1; break;
        case 1: sum +=2; break;
        case 2: sum +=3; break;
    }
}
```

{{ select(13) }}

- A. 10
- B. 11
- C. 14
- D. 15

---

14. 以下关于C++中abs()函数的描述，正确的是( )

{{ select(14) }}

- A. abs()函数可用于计算int类型整数的绝对值，头文件为<cmath>或<cstdlib>
- B. 调用abs(3.14)可以四舍五入得到3
- C. abs(-1, -2)的返回值是1
- D. 若传入负数浮点数，abs()会自动截断小数部分后返回整数绝对值

---

15. 如果字符串s的值是"GESP"，以下代码执行后s的最后结果是( )

```cpp
for (int i = 0; i < s.length(); i++) {
    s[i] = toupper(s[i] + i);
}
```

{{ select(15) }}

- A. GESP
- B. HFTR
- C. hesp
- D. GFUS

---

## 二、判断题（每题2分，共20分）

16. 在C++中，数组名不能改变，数组名不能被赋值｡( )

{{ select(16) }}

- A. 正确
- B. 错误

---

17. 整型变量a的值为3，浮点数变量b的值为3.5，a *= b后a的值变为10.5，输出a将输出10.5｡( )

{{ select(17) }}

- A. 正确
- B. 错误

---

18. strcmp(str1, str2)返回0表示str1大于str2，返回正数表示两者相等｡( )

{{ select(18) }}

- A. 正确
- B. 错误

---

19. 以下代码输出结果为8：

```cpp
int a=5, b=3;
int c = a++ + ++b;
cout << c << endl;
```

{{ select(19) }}

- A. 正确
- B. 错误

---

20. 位运算符&、|、^、~的优先级高于算术运算符+、-、*、/｡( )

{{ select(20) }}

- A. 正确
- B. 错误

---

21. 在C++中，int a[] = {1,2,3,4,5}; 可以定义一个包含5个元素的整型数组｡( )

{{ select(21) }}

- A. 正确
- B. 错误

---

22. 以下代码执行后，输出将会是2：

```cpp
int main() {
    int arr[5] = {1,2,3,4,5};
    cout << arr[3>2 ? 1 : 2] << endl;
    return 0;
}
```

{{ select(22) }}

- A. 正确
- B. 错误

---

23. 在C++中，++和--运算符只能用于整型变量，不能用于浮点数变量｡( )

{{ select(23) }}

- A. 正确
- B. 错误

---

24. 给定正整数a，计算-a的补码时，可将a的二进制从右往左扫描，遇到第一个1后，将该1左边所有位取反，得到-a的补码｡( )

{{ select(24) }}

- A. 正确
- B. 错误

---

25. 以下代码中，Hello将被输出5次：

```cpp
for (int i=0; i<5; i++); {
    cout << "Hello" << endl;
}
```

{{ select(25) }}

- A. 正确
- B. 错误

# 2023年6月 C++三级考试真题

## 一、单选题（每题2分，共30分）

1. 高级语言编写的程序需要经过以下( )操作，可以生成在计算机上运行的可执行代码｡

{{ select(1) }}

- A. 编辑
- B. 保存
- C. 调试
- D. 编译

---

2. 二进制数11.01在十进制下是( )｡

{{ select(2) }}

- A. 3.01
- B. 3.05
- C. 3.125
- D. 3.25

---

3. 已知大写字符'A'的ASCII编码十六进制表示为0x41，则字符'F'的ASCII编码十六进制表示为( )｡

{{ select(3) }}

- A. 46
- B. 47
- C. 48
- D. 49

---

4. 以下哪个不是C++语言中的运算符?( )

{{ select(4) }}

- A. &
- B. &&
- C. *
- D. **

---

5. 如果字符串定义为`char str[] = "Hello";`，则字符数组str的长度为( )｡

{{ select(5) }}

- A. 0
- B. 5
- C. 6
- D. 7

---

6. 一个数组定义为`double array[3];`，则这个数组占用内存的大小为( )｡

{{ select(6) }}

- A. 24
- B. 12
- C. 6
- D. 3

---

7. 以下数组定义，符合C++语言语法的是( )｡

{{ select(7) }}

- A. int a[];
- B. int b['3'];
- C. int c[3.0];
- D. int[3] d;

---

8. 下列关于进制的叙述，不正确的是( )｡

{{ select(8) }}

- A. 正整数的二进制表示中只会出现0和1｡
- B. 10不是2的整数次幂，所以十进制数无法转换为二进制数｡
- C. 二进制转8进制可由低到高每3位转换为1位8进制位｡
- D. 二进制转16进制可由低到高每4位转换为1位16进制位｡

---

9. 下列关于C++语言中数组的叙述，不正确的是( )｡

{{ select(9) }}

- A. 数组必须先定义后使用｡
- B. 数组的所有元素在内存中是连续存放的｡
- C. 除了字符数组，定义数组时"[]"内必须有常数｡
- D. 不能对数组赋值，但可以对数组的每个基础类型元素赋值｡

---

10. 一个int类型的值，做以下哪个操作，一定会变回原来的值?( )

{{ select(10) }}

- A. 左移3位，再右移3位｡
- B. 右移3位，再左移3位｡
- C. 按位或7，再按位与-8｡
- D. 按位异或7，再按位异或7｡

---

11. 如果a和b均为int类型的变量，下列表达式能正确判断"a等于b"的是( )｡

{{ select(11) }}

- A. (a / b) == 1
- B. (a & b) == a
- C. (a ^ b) == 0
- D. (a | b) == b

---

12. 如果a为int类型的变量，下列哪个表达式可以正确求出"小于等于a且是4的倍数"的整数中最大的?( )

{{ select(12) }}

- A. (a & (~3))
- B. ((a << 2) >> 2)
- C. (a ^ 3)
- D. ((a - 1) | 3) + 1

---

13. 在下列代码的横线处填写( )，可以使得输出是"24 12"｡

```cpp
#include<iostream>
using namespace std;
int main(){
    int a=12,b=24;
    ____; // 横线处
    a = a^b;
    b = a^b;
    cout << a <<" "<<b << endl;
    return 0;
}
```

{{ select(13) }}

- A. a = a ^ b
- B. b = a ^ b
- C. a = a + b
- D. b = a + b

---

14. 在下列代码的横线处填写( )，可以使得输出是"2"｡

```cpp
#include<iostream>
using namespace std;
int main(){
    int array[5]= {3,7,5,2,4};
    int min=0;
    for (int i=0;i<5;i++)
        if(____) // 横线处
            min = array[i];
    cout << min <<endl;
    return 0;
}
```

{{ select(14) }}

- A. min > array[i]
- B. min < array[i]
- C. min = array[i]
- D. 以上均不对

---

15. 在下列代码的横线处填写( )，可以使得输出不是"31"｡

```cpp
#include<iostream>
using namespace std;
int main(){
    int array[5] = {1,2,4,8,16};
    int res=0;
    for (int i=0;i<5;i++)
        ____; // 横线处
    cout << res <<endl;
    return 0;
}
```

{{ select(15) }}

- A. res = res + array[i]
- B. res = res & array[i]
- C. res = res | array[i]
- D. res = res ^ array[i]

---

## 二、判断题（每题2分，共20分）

16. 一个算法可以用不同形式描述，但要求规范，因此不能用自然语言描述｡

{{ select(16) }}

- A. 正确
- B. 错误

---

17. 域名gesp.ccf.org.cn的顶级域名是gesp｡

{{ select(17) }}

- A. 正确
- B. 错误

---

18. 数据编码方式只有原码、反码、补码三种｡

{{ select(18) }}

- A. 正确
- B. 错误

---

19. 在C++中，长度为n的数组，合理下标范围是0到n（含0和n）｡

{{ select(19) }}

- A. 正确
- B. 错误

---

20. 字符常量'\0'常用来表示字符串结束，它和字符常量'0'是不同的｡

{{ select(20) }}

- A. 正确
- B. 错误

---

21. 在C++中，可以使用字符（如'0'）作为数组下标｡

{{ select(21) }}

- A. 正确
- B. 错误

---

22. 在C++中，数组被定义时，它的大小就确定了｡

{{ select(22) }}

- A. 正确
- B. 错误

---

23. 计算机中存储的数据都是二进制形式，因此C++程序中把所有十进制数改为二进制数，会使程序运行效率更高｡

{{ select(23) }}

- A. 正确
- B. 错误

---

24. 在C++中，表达式`(0xf == 015)`的值为true｡

{{ select(24) }}

- A. 正确
- B. 错误

---

25. 如果a为int类型变量，且表达式`((a | 3) == 3)`的值为true，则说明a在0到3之间（可能为0或3）｡

{{ select(25) }}

- A. 正确
- B. 错误

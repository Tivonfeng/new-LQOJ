/**
 * Avatar Settings Plugin
 *
 * This plugin provides a modern React-based UI for managing user avatars.
 * It supports multiple avatar sources: Gravatar, Github, QQ, direct uploads, and preset avatars.
 *
 * Features:
 * - Modern React interface replacing the original form
 * - Support for multiple avatar providers (Gravatar, Github, QQ, presets)
 * - Direct avatar file upload with validation
 * - 30 configurable preset avatars
 * - International language support
 *
 * Note: Static images are served automatically by Hydro from the plugin's public/ directory
 * at the root level (e.g., /avatar_preset_1.jpeg)
 */

export default function apply() {
    console.log('[Avatar Settings Plugin] Loaded successfully!');
}

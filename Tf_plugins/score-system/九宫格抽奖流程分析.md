# 九宫格抽奖游戏流程与奖项分析

## 📋 目录
1. [游戏概述](#游戏概述)
2. [奖项配置分析](#奖项配置分析)
3. [完整流程分析](#完整流程分析)
4. [数据流转](#数据流转)
5. [优化建议](#优化建议)

---

## 🎮 游戏概述

九宫格抽奖是一个基于积分的娱乐游戏，用户每次消耗 50 积分进行抽奖，有机会获得不同价值的积分奖励。

### 基本信息
- **投注金额**: 50 积分/次
- **每日限制**: 通过 `DailyGameLimitService` 管理（默认配置在相关服务中）
- **游戏类型**: 九宫格转盘抽奖
- **前端组件**: 使用 `@lucky-canvas/react` 的 `LuckyGrid` 组件

---

## 🎁 奖项配置分析

### 当前奖项配置（硬编码在 `LotteryService.ts`）

```typescript
private static readonly PRIZES = [
    { name: '10积分', type: 'reward', reward: 10, probability: 0.15 },  // 15%
    { name: '20积分', type: 'reward', reward: 20, probability: 0.10 },  // 10%
    { name: '50积分', type: 'reward', reward: 50, probability: 0.05 },  // 5%
    { name: '100积分', type: 'reward', reward: 100, probability: 0.02 }, // 2%
    { name: '谢谢参与', type: 'nothing', reward: 0, probability: 0.50 },   // 50%
    { name: '5积分', type: 'reward', reward: 5, probability: 0.10 },   // 10%
    { name: '30积分', type: 'reward', reward: 30, probability: 0.05 },  // 5%
    { name: '200积分', type: 'reward', reward: 200, probability: 0.02 }, // 2%
    { name: '再来一次', type: 'bonus', reward: 0, probability: 0.01 },    // 1%
];
```

### 奖项统计

| 奖项名称 | 类型 | 奖励积分 | 概率 | 净收益 | 期望值贡献 |
|---------|------|---------|------|--------|-----------|
| 10积分 | reward | 10 | 15% | -40 | -6.0 |
| 20积分 | reward | 20 | 10% | -30 | -3.0 |
| 50积分 | reward | 50 | 5% | 0 | 0.0 |
| 100积分 | reward | 100 | 2% | +50 | +1.0 |
| 谢谢参与 | nothing | 0 | 50% | -50 | -25.0 |
| 5积分 | reward | 5 | 10% | -45 | -4.5 |
| 30积分 | reward | 30 | 5% | -20 | -1.0 |
| 200积分 | reward | 200 | 2% | +150 | +3.0 |
| 再来一次 | bonus | 0* | 1% | 0 | 0.0 |

*再来一次会返还投注，相当于净收益为 0

### 概率验证

**总概率**: 0.15 + 0.10 + 0.05 + 0.02 + 0.50 + 0.10 + 0.05 + 0.02 + 0.01 = **1.00** ✅

### 期望值计算

**每次抽奖的期望净收益**:
```
E(净收益) = Σ(概率 × 净收益)
         = 0.15×(-40) + 0.10×(-30) + 0.05×0 + 0.02×50 + 0.50×(-50) 
         + 0.10×(-45) + 0.05×(-20) + 0.02×150 + 0.01×0
         = -6.0 - 3.0 + 0 + 1.0 - 25.0 - 4.5 - 1.0 + 3.0 + 0
         = -35.5 积分
```

**结论**: 每次抽奖平均亏损 35.5 积分，长期来看对系统有利。

### 中奖率分析

**有奖励的中奖率** = 1 - 0.50 - 0.01 = **49%**
- 实际积分奖励: 47% (排除"再来一次")
- 谢谢参与: 50%
- 再来一次: 1%

---

## 🔄 完整流程分析

### 1. 用户访问游戏页面

**路由**: `/score/lottery`  
**处理器**: `LotteryGameHandler.get()`

**执行步骤**:
1. 检查用户登录状态
2. 获取用户当前积分 (`ScoreService.getUserScore()`)
3. 获取用户游戏统计 (`LotteryService.getUserLotteryStats()`)
4. 获取最近 10 条游戏记录 (`LotteryService.getUserGameHistory()`)
5. 检查每日游戏次数限制 (`DailyGameLimitService.checkCanPlay()`)
6. 获取游戏配置 (`LotteryService.getGameConfig()`)
7. 判断是否可以抽奖: `currentCoins >= betAmount && dailyLimit.canPlay`
8. 渲染前端页面，传递数据到 React 组件

### 2. 用户点击抽奖按钮

**前端流程** (`lottery-game.page.tsx`):

```typescript
handleStart() {
  1. 检查游戏状态和权限
  2. 启动 LuckyGrid 动画 (luckyGridRef.current.play())
  3. 发送 POST 请求到 /score/lottery/play
  4. 等待后端响应
  5. 收到结果后，延迟 2.5 秒停止动画
  6. 调用 luckyGridRef.current.stop(prizeIndex)
}
```

### 3. 后端处理抽奖请求

**路由**: `/score/lottery/play`  
**处理器**: `LotteryPlayHandler.post()`

**执行步骤**:
1. ✅ 检查每日游戏次数限制
2. ✅ 创建 `LotteryService` 实例
3. ✅ 调用 `lotteryService.playLottery(domainId, uid)`

### 4. 抽奖核心逻辑

**服务**: `LotteryService.playLottery()`

**详细流程**:

```typescript
playLottery(domainId, uid) {
  // 步骤 1: 检查积分
  userScore = getUserScore(domainId, uid)
  if (userScore < 50) {
    return { success: false, message: "积分不足" }
  }

  // 步骤 2: 抽取奖品
  prizeIndex = drawPrize()  // 根据概率随机抽取
  prize = PRIZES[prizeIndex]

  // 步骤 3: 计算净收益
  netGain = -50  // 先扣除投注
  if (prize.type === 'reward') {
    netGain += prize.reward
  } else if (prize.type === 'bonus') {
    netGain = 0  // 再来一次，返还投注
  }
  // nothing 类型保持 netGain = -50

  // 步骤 4: 保存游戏记录
  gameRecord = {
    uid, domainId,
    bet: 50,
    prizeIndex, prizeName: prize.name,
    prizeType: prize.type,
    reward: prize.reward,
    netGain,
    gameTime: new Date()
  }
  recordId = insertOne('lottery.records', gameRecord)

  // 步骤 5: 扣除投注积分
  updateUserScore(domainId, uid, -50)
  addScoreRecord({
    score: -50,
    reason: "九宫格抽奖投注50积分",
    category: GAME_ENTERTAINMENT
  })

  // 步骤 6: 发放奖励（如果有）
  if (prize.reward > 0) {
    updateUserScore(domainId, uid, prize.reward)
    addScoreRecord({
      score: prize.reward,
      reason: `九宫格抽奖获得${prize.name}`
    })
  }

  // 步骤 7: 处理"再来一次"
  if (prize.type === 'bonus') {
    updateUserScore(domainId, uid, 50)  // 返还投注
    addScoreRecord({
      score: 50,
      reason: "九宫格抽奖获得再来一次，返还投注"
    })
  }

  // 步骤 8: 更新用户统计
  updateUserStats(domainId, uid, won, netGain, 50, prize.reward)

  // 步骤 9: 返回结果
  return { success: true, result: gameRecord }
}
```

### 5. 抽奖算法

**方法**: `LotteryService.drawPrize()`

```typescript
drawPrize() {
  random = Math.random()  // 0.0 ~ 1.0
  cumulative = 0

  for (i = 0; i < PRIZES.length; i++) {
    cumulative += PRIZES[i].probability
    if (random <= cumulative) {
      return i
    }
  }

  return 4  // 默认返回"谢谢参与"
}
```

**示例**:
- 如果 `random = 0.12`，则返回索引 0 (10积分，累积概率 0.15)
- 如果 `random = 0.85`，则返回索引 4 (谢谢参与，累积概率 0.50)
- 如果 `random = 0.99`，则返回索引 8 (再来一次，累积概率 1.00)

### 6. 前端显示结果

**回调**: `handleEnd()` (动画停止后触发)

```typescript
handleEnd() {
  1. 获取抽奖结果 (lotteryResultRef.current)
  2. 显示结果卡片
  3. 更新当前积分显示
  4. 显示成功/失败消息
  5. 2 秒后刷新游戏数据 (refreshGameData())
  6. 5 秒后自动关闭结果卡片
}
```

---

## 📊 数据流转

### 数据库集合

1. **`lottery.records`** - 游戏记录
   ```typescript
   {
     _id: ObjectId,
     uid: number,
     domainId: string,
     bet: number,           // 投注金额
     prizeIndex: number,    // 奖品索引 (0-8)
     prizeName: string,     // 奖品名称
     prizeType: 'reward' | 'nothing' | 'bonus',
     reward: number,       // 奖励积分
     netGain: number,       // 净收益
     gameTime: Date
   }
   ```

2. **`lottery.stats`** - 用户统计
   ```typescript
   {
     _id: ObjectId,
     uid: number,
     domainId: string,
     totalGames: number,    // 总抽奖次数
     totalWins: number,     // 总中奖次数
     totalBet: number,      // 总投入
     totalReward: number,   // 总奖励
     netProfit: number,     // 净盈亏
     lastGameTime: Date
   }
   ```

3. **`score.records`** - 积分记录（关联抽奖）
   - 投注扣除: `score: -50`, `category: GAME_ENTERTAINMENT`
   - 奖励发放: `score: prize.reward`, `category: GAME_ENTERTAINMENT`
   - 再来一次返还: `score: 50`, `category: GAME_ENTERTAINMENT`

4. **`score.users`** - 用户积分汇总
   - 每次抽奖后更新 `totalScore`

### API 端点

| 路由 | 方法 | 功能 | 返回格式 |
|------|------|------|---------|
| `/score/lottery` | GET | 游戏主页面 | HTML |
| `/score/lottery/status` | GET | 获取游戏状态 | JSON |
| `/score/lottery/play` | POST | 执行抽奖 | JSON |
| `/score/lottery/history` | GET | 获取历史记录 | JSON |

---

## 💡 优化建议

### 1. 奖项配置优化

#### 问题
- ❌ 奖项配置硬编码在代码中，无法动态调整
- ❌ 没有管理后台可以修改奖项
- ❌ 概率总和需要手动验证

#### 建议方案

**方案 A: 配置文件化**
```typescript
// 在 config.ts 或单独配置文件
export const LOTTERY_CONFIG = {
  betAmount: 50,
  prizes: [
    { name: '10积分', type: 'reward', reward: 10, probability: 0.15 },
    // ...
  ]
}
```

**方案 B: 数据库存储（推荐）**
```typescript
// 创建 lottery.config 集合
{
  _id: ObjectId,
  domainId: string,
  betAmount: number,
  prizes: Array<{
    name: string,
    type: 'reward' | 'nothing' | 'bonus',
    reward: number,
    probability: number
  }>,
  updatedAt: Date,
  updatedBy: number  // 管理员UID
}
```

**方案 C: 管理后台界面**
- 创建 `/score/lottery/admin` 路由
- 提供可视化界面编辑奖项
- 实时验证概率总和
- 支持启用/禁用单个奖项

### 2. 奖项平衡性优化

#### 当前问题
- 期望值 -35.5 积分，对用户不太友好
- 50% 的"谢谢参与"概率较高，用户体验差

#### 优化建议

**方案 1: 提高中奖率**
```typescript
// 降低"谢谢参与"概率，提高小奖概率
{ name: '谢谢参与', probability: 0.40 },  // 从 50% 降到 40%
{ name: '10积分', probability: 0.20 },    // 从 15% 提升到 20%
{ name: '5积分', probability: 0.15 },     // 从 10% 提升到 15%
```

**方案 2: 调整奖励金额**
```typescript
// 增加保底奖励，减少亏损
{ name: '保底5积分', reward: 5, probability: 0.30 },  // 新增
{ name: '谢谢参与', probability: 0.30 },              // 降低到 30%
```

**方案 3: 优化期望值**
```typescript
// 目标: 期望值控制在 -20 ~ -30 之间
// 既保证系统盈利，又提升用户体验
```

### 3. 功能增强

#### 建议添加的功能

1. **保底机制**
   - 连续 N 次未中奖后，下次必中奖
   - 或累计抽奖 N 次后，获得保底奖励

2. **特殊活动**
   - 限时双倍奖励
   - 节日特殊奖品
   - 概率提升活动

3. **统计优化**
   - 显示期望值信息
   - 显示历史中奖分布
   - 显示最佳/最差运气记录

4. **用户体验**
   - 抽奖动画优化
   - 音效反馈
   - 中奖特效

### 4. 代码结构优化

#### 当前问题
- 奖项配置分散在多个地方
- 缺少配置验证逻辑

#### 建议重构

```typescript
// 创建 LotteryConfigService
class LotteryConfigService {
  // 验证配置
  validateConfig(config: LotteryConfig): ValidationResult {
    const totalProb = config.prizes.reduce((sum, p) => sum + p.probability, 0);
    if (Math.abs(totalProb - 1.0) > 0.001) {
      return { valid: false, error: '概率总和必须等于 1.0' };
    }
    // 其他验证...
  }

  // 计算期望值
  calculateExpectedValue(config: LotteryConfig): number {
    // ...
  }
}
```

---

## 📝 总结

### 当前状态
- ✅ 功能完整，流程清晰
- ✅ 数据记录完善
- ✅ 前端交互良好
- ⚠️ 奖项配置硬编码
- ⚠️ 期望值对用户不够友好

### 优先级建议

1. **高优先级**: 将奖项配置改为可配置（数据库或配置文件）
2. **中优先级**: 优化奖项平衡性，提升用户体验
3. **低优先级**: 添加管理后台和高级功能

### 下一步行动

1. 分析当前奖项配置的合理性
2. 设计配置管理方案
3. 实现配置可动态调整
4. 优化奖项概率和奖励金额
5. 添加管理后台界面（可选）

---

**文档生成时间**: 2024年  
**最后更新**: 分析当前代码版本
